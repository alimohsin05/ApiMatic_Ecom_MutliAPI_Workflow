/**
 * Slack Web APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */
import { defaultSuccessTemplateSchema, } from '../models/defaultSuccessTemplate.js';
import { number, optional, string } from '../schema.js';
import { BaseApi } from './baseApi.js';
import { DefaultErrorTemplateError } from '../errors/defaultErrorTemplateError.js';
export class AdminAppsRestrictedApi extends BaseApi {
    /**
     * List restricted apps for an org or workspace.
     *
     * @param token         Authentication token. Requires scope: `admin.apps:read`
     * @param limit         The maximum number of items to return. Must be between 1 - 1000 both inclusive.
     * @param cursor        Set `cursor` to `next_cursor` returned by the previous call to list items in the
     *                                next page
     * @param teamId
     * @param enterpriseId
     * @return Response from the API call
     */
    async adminAppsRestrictedList(token, limit, cursor, teamId, enterpriseId, requestOptions) {
        const req = this.createRequest('GET', '/admin.apps.restricted.list');
        const mapped = req.prepareArgs({
            token: [token, string()],
            limit: [limit, optional(number())],
            cursor: [cursor, optional(string())],
            teamId: [teamId, optional(string())],
            enterpriseId: [enterpriseId, optional(string())],
        });
        req.header('Content-Type', 'application/x-www-form-urlencoded');
        req.query('token', mapped.token);
        req.query('limit', mapped.limit);
        req.query('cursor', mapped.cursor);
        req.query('team_id', mapped.teamId);
        req.query('enterprise_id', mapped.enterpriseId);
        req.defaultToError(DefaultErrorTemplateError, 'Typical error response');
        req.authenticate([{ slackAuth: true }]);
        return req.callAsJson(defaultSuccessTemplateSchema, requestOptions);
    }
}
//# sourceMappingURL=adminAppsRestrictedApi.js.map