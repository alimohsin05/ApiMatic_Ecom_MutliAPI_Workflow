/**
 * Twilio APIsLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */
import { workflowStatisticsSchema, } from '../models/workflowStatistics.js';
import { number, optional, string } from '../schema.js';
import { BaseApi } from './baseApi.js';
export class TaskrouterV1WorkflowStatisticsApi extends BaseApi {
    /**
     * @param workspaceSid    The SID of the Workspace with the Workflow to fetch.
     * @param workflowSid     Returns the list of Tasks that are being controlled by the Workflow with the
     *                                  specified SID value.
     * @param minutes         Only calculate statistics since this many minutes in the past. The default 15
     *                                  minutes. This is helpful for displaying statistics for the last 15 minutes, 240
     *                                  minutes (4 hours), and 480 minutes (8 hours) to see trends.
     * @param startDate       Only calculate statistics from this date and time and later, specified in [ISO
     *                                  8601](https://en.wikipedia.org/wiki/ISO_8601) format.
     * @param endDate         Only calculate statistics from this date and time and earlier, specified in GMT
     *                                  as an [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date-time.
     * @param taskChannel     Only calculate real-time statistics on this TaskChannel. Can be the
     *                                  TaskChannel's SID or its `unique_name`, such as `voice`, `sms`, or `default`.
     * @param splitByWaitTime A comma separated list of values that describes the thresholds, in seconds, to
     *                                  calculate statistics on. For each threshold specified, the number of Tasks
     *                                  canceled and reservations accepted above and below the specified thresholds in
     *                                  seconds are computed. For example, `5,30` would show splits of Tasks that were
     *                                  canceled or accepted before and after 5 seconds and before and after 30 seconds.
     *                                  This can be used to show short abandoned Tasks or Tasks that failed to meet an
     *                                  SLA.
     * @return Response from the API call
     */
    async fetchWorkflowStatistics(workspaceSid, workflowSid, minutes, startDate, endDate, taskChannel, splitByWaitTime, requestOptions) {
        const req = this.createRequest('GET');
        req.baseUrl('default4');
        const mapped = req.prepareArgs({
            workspaceSid: [workspaceSid, string()],
            workflowSid: [workflowSid, string()],
            minutes: [minutes, optional(number())],
            startDate: [startDate, optional(string())],
            endDate: [endDate, optional(string())],
            taskChannel: [taskChannel, optional(string())],
            splitByWaitTime: [splitByWaitTime, optional(string())],
        });
        req.query('Minutes', mapped.minutes);
        req.query('StartDate', mapped.startDate);
        req.query('EndDate', mapped.endDate);
        req.query('TaskChannel', mapped.taskChannel);
        req.query('SplitByWaitTime', mapped.splitByWaitTime);
        req.appendTemplatePath `/v1/Workspaces/${mapped.workspaceSid}/Workflows/${mapped.workflowSid}/Statistics`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(workflowStatisticsSchema, requestOptions);
    }
}
//# sourceMappingURL=taskrouterV1WorkflowStatisticsApi.js.map