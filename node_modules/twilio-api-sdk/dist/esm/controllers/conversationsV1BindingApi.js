/**
 * Twilio APIsLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */
import { listServiceBindingResponseSchema, } from '../models/listServiceBindingResponse.js';
import { serviceBindingSchema, } from '../models/serviceBinding.js';
import { serviceBindingBindingTypeSchema, } from '../models/serviceBindingBindingType.js';
import { array, bigint, number, optional, string } from '../schema.js';
import { BaseApi } from './baseApi.js';
export class ConversationsV1BindingApi extends BaseApi {
    /**
     * Remove a push notification binding from the conversation service
     *
     * @param chatServiceSid The SID of the [Conversation Service](https://www.twilio.
     *                                 com/docs/conversations/api/service-resource) to delete the Binding resource from.
     * @param sid            The SID of the Binding resource to delete.
     * @return Response from the API call
     */
    async deleteServiceBinding(chatServiceSid, sid, requestOptions) {
        const req = this.createRequest('DELETE');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            chatServiceSid: [chatServiceSid, string()],
            sid: [sid, string()],
        });
        req.appendTemplatePath `/v1/Services/${mapped.chatServiceSid}/Bindings/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.call(requestOptions);
    }
    /**
     * Fetch a push notification binding from the conversation service
     *
     * @param chatServiceSid The SID of the [Conversation Service](https://www.twilio.
     *                                 com/docs/conversations/api/service-resource) the Binding resource is associated
     *                                 with.
     * @param sid            A 34 character string that uniquely identifies this resource.
     * @return Response from the API call
     */
    async fetchServiceBinding(chatServiceSid, sid, requestOptions) {
        const req = this.createRequest('GET');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            chatServiceSid: [chatServiceSid, string()],
            sid: [sid, string()],
        });
        req.appendTemplatePath `/v1/Services/${mapped.chatServiceSid}/Bindings/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(serviceBindingSchema, requestOptions);
    }
    /**
     * Retrieve a list of all push notification bindings in the conversation service
     *
     * @param chatServiceSid The SID of the [Conversation Service](https://www.twilio.
     *                                                      com/docs/conversations/api/service-resource) the Binding
     *                                                      resource is associated with.
     * @param bindingType    The push technology used by the Binding resources to read.
     *                                                      Can be: `apn`, `gcm`, `fcm`, or `twilsock`.  See [push
     *                                                      notification configuration](https://www.twilio.
     *                                                      com/docs/chat/push-notification-configuration) for more info.
     * @param identity       The identity of a [Conversation User](https://www.twilio.
     *                                                      com/docs/conversations/api/user-resource) this binding
     *                                                      belongs to. See [access tokens](https://www.twilio.
     *                                                      com/docs/conversations/create-tokens) for more details.
     * @param pageSize       How many resources to return in each list page. The default
     *                                                      is 50, and the maximum is 100.
     * @param page           The page index. This value is simply for client state.
     * @param pageToken      The page token. This is provided by the API.
     * @return Response from the API call
     */
    async listServiceBinding(chatServiceSid, bindingType, identity, pageSize, page, pageToken, requestOptions) {
        const req = this.createRequest('GET');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            chatServiceSid: [chatServiceSid, string()],
            bindingType: [
                bindingType,
                optional(array(serviceBindingBindingTypeSchema)),
            ],
            identity: [identity, optional(array(string()))],
            pageSize: [pageSize, optional(bigint())],
            page: [page, optional(number())],
            pageToken: [pageToken, optional(string())],
        });
        req.query('BindingType', mapped.bindingType);
        req.query('Identity', mapped.identity);
        req.query('PageSize', mapped.pageSize);
        req.query('Page', mapped.page);
        req.query('PageToken', mapped.pageToken);
        req.appendTemplatePath `/v1/Services/${mapped.chatServiceSid}/Bindings`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(listServiceBindingResponseSchema, requestOptions);
    }
}
//# sourceMappingURL=conversationsV1BindingApi.js.map