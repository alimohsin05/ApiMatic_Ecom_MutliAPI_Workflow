/**
 * Twilio APIsLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */
import { listWorkflowResponseSchema, } from '../models/listWorkflowResponse.js';
import { workflowSchema } from '../models/workflow.js';
import { bigint, number, optional, string } from '../schema.js';
import { BaseApi } from './baseApi.js';
export class TaskrouterV1WorkflowApi extends BaseApi {
    /**
     * @param workspaceSid The SID of the Workspace with the Workflow to fetch.
     * @param sid          The SID of the Workflow resource to fetch.
     * @return Response from the API call
     */
    async fetchWorkflow(workspaceSid, sid, requestOptions) {
        const req = this.createRequest('GET');
        req.baseUrl('default4');
        const mapped = req.prepareArgs({
            workspaceSid: [workspaceSid, string()],
            sid: [sid, string()],
        });
        req.appendTemplatePath `/v1/Workspaces/${mapped.workspaceSid}/Workflows/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(workflowSchema, requestOptions);
    }
    /**
     * @param workspaceSid                  The SID of the Workspace with the Workflow to update.
     * @param sid                           The SID of the Workflow resource to update.
     * @param friendlyName                  A descriptive string that you create to describe the Workflow
     *                                                resource. For example, `Inbound Call Workflow` or `2014 Outbound
     *                                                Campaign`.
     * @param assignmentCallbackUrl         The URL from your application that will process task assignment
     *                                                events. See [Handling Task Assignment Callback](https://www.twilio.
     *                                                com/docs/taskrouter/handle-assignment-callbacks) for more details.
     * @param fallbackAssignmentCallbackUrl The URL that we should call when a call to the
     *                                                `assignment_callback_url` fails.
     * @param configuration                 A JSON string that contains the rules to apply to the Workflow.
     *                                                See [Configuring Workflows](https://www.twilio.
     *                                                com/docs/taskrouter/workflow-configuration) for more information.
     * @param taskReservationTimeout        How long TaskRouter will wait for a confirmation response from
     *                                                your application after it assigns a Task to a Worker. Can be up to
     *                                                `86,400` (24 hours) and the default is `120`.
     * @param reEvaluateTasks               Whether or not to re-evaluate Tasks. The default is `false`, which
     *                                                means Tasks in the Workflow will not be processed through the
     *                                                assignment loop again.
     * @return Response from the API call
     */
    async updateWorkflow(workspaceSid, sid, friendlyName, assignmentCallbackUrl, fallbackAssignmentCallbackUrl, configuration, taskReservationTimeout, reEvaluateTasks, requestOptions) {
        const req = this.createRequest('POST');
        req.baseUrl('default4');
        const mapped = req.prepareArgs({
            workspaceSid: [workspaceSid, string()],
            sid: [sid, string()],
            friendlyName: [friendlyName, optional(string())],
            assignmentCallbackUrl: [assignmentCallbackUrl, optional(string())],
            fallbackAssignmentCallbackUrl: [
                fallbackAssignmentCallbackUrl,
                optional(string()),
            ],
            configuration: [configuration, optional(string())],
            taskReservationTimeout: [taskReservationTimeout, optional(number())],
            reEvaluateTasks: [reEvaluateTasks, optional(string())],
        });
        req.header('Content-Type', 'application/x-www-form-urlencoded');
        req.form({
            FriendlyName: mapped.friendlyName,
            AssignmentCallbackUrl: mapped.assignmentCallbackUrl,
            FallbackAssignmentCallbackUrl: mapped.fallbackAssignmentCallbackUrl,
            Configuration: mapped.configuration,
            TaskReservationTimeout: mapped.taskReservationTimeout,
            ReEvaluateTasks: mapped.reEvaluateTasks,
        });
        req.appendTemplatePath `/v1/Workspaces/${mapped.workspaceSid}/Workflows/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(workflowSchema, requestOptions);
    }
    /**
     * @param workspaceSid The SID of the Workspace with the Workflow to delete.
     * @param sid          The SID of the Workflow resource to delete.
     * @return Response from the API call
     */
    async deleteWorkflow(workspaceSid, sid, requestOptions) {
        const req = this.createRequest('DELETE');
        req.baseUrl('default4');
        const mapped = req.prepareArgs({
            workspaceSid: [workspaceSid, string()],
            sid: [sid, string()],
        });
        req.appendTemplatePath `/v1/Workspaces/${mapped.workspaceSid}/Workflows/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.call(requestOptions);
    }
    /**
     * @param workspaceSid The SID of the Workspace with the Workflow to read.
     * @param friendlyName The `friendly_name` of the Workflow resources to read.
     * @param pageSize     How many resources to return in each list page. The default is 50, and the maximum
     *                               is 1000.
     * @param page         The page index. This value is simply for client state.
     * @param pageToken    The page token. This is provided by the API.
     * @return Response from the API call
     */
    async listWorkflow(workspaceSid, friendlyName, pageSize, page, pageToken, requestOptions) {
        const req = this.createRequest('GET');
        req.baseUrl('default4');
        const mapped = req.prepareArgs({
            workspaceSid: [workspaceSid, string()],
            friendlyName: [friendlyName, optional(string())],
            pageSize: [pageSize, optional(bigint())],
            page: [page, optional(number())],
            pageToken: [pageToken, optional(string())],
        });
        req.query('FriendlyName', mapped.friendlyName);
        req.query('PageSize', mapped.pageSize);
        req.query('Page', mapped.page);
        req.query('PageToken', mapped.pageToken);
        req.appendTemplatePath `/v1/Workspaces/${mapped.workspaceSid}/Workflows`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(listWorkflowResponseSchema, requestOptions);
    }
    /**
     * @param workspaceSid                  The SID of the Workspace that the new Workflow to create belongs
     *                                                to.
     * @param friendlyName                  A descriptive string that you create to describe the Workflow
     *                                                resource. For example, `Inbound Call Workflow` or `2014 Outbound
     *                                                Campaign`.
     * @param configuration                 A JSON string that contains the rules to apply to the Workflow.
     *                                                See [Configuring Workflows](https://www.twilio.
     *                                                com/docs/taskrouter/workflow-configuration) for more information.
     * @param assignmentCallbackUrl         The URL from your application that will process task assignment
     *                                                events. See [Handling Task Assignment Callback](https://www.twilio.
     *                                                com/docs/taskrouter/handle-assignment-callbacks) for more details.
     * @param fallbackAssignmentCallbackUrl The URL that we should call when a call to the
     *                                                `assignment_callback_url` fails.
     * @param taskReservationTimeout        How long TaskRouter will wait for a confirmation response from
     *                                                your application after it assigns a Task to a Worker. Can be up to
     *                                                `86,400` (24 hours) and the default is `120`.
     * @return Response from the API call
     */
    async createWorkflow(workspaceSid, friendlyName, configuration, assignmentCallbackUrl, fallbackAssignmentCallbackUrl, taskReservationTimeout, requestOptions) {
        const req = this.createRequest('POST');
        req.baseUrl('default4');
        const mapped = req.prepareArgs({
            workspaceSid: [workspaceSid, string()],
            friendlyName: [friendlyName, string()],
            configuration: [configuration, string()],
            assignmentCallbackUrl: [assignmentCallbackUrl, optional(string())],
            fallbackAssignmentCallbackUrl: [
                fallbackAssignmentCallbackUrl,
                optional(string()),
            ],
            taskReservationTimeout: [taskReservationTimeout, optional(number())],
        });
        req.header('Content-Type', 'application/x-www-form-urlencoded');
        req.form({
            FriendlyName: mapped.friendlyName,
            Configuration: mapped.configuration,
            AssignmentCallbackUrl: mapped.assignmentCallbackUrl,
            FallbackAssignmentCallbackUrl: mapped.fallbackAssignmentCallbackUrl,
            TaskReservationTimeout: mapped.taskReservationTimeout,
        });
        req.appendTemplatePath `/v1/Workspaces/${mapped.workspaceSid}/Workflows`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(workflowSchema, requestOptions);
    }
}
//# sourceMappingURL=taskrouterV1WorkflowApi.js.map