/**
 * Twilio APIsLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */
import { activitySchema } from '../models/activity.js';
import { listActivityResponseSchema, } from '../models/listActivityResponse.js';
import { bigint, boolean, number, optional, string } from '../schema.js';
import { BaseApi } from './baseApi.js';
export class TaskrouterV1ActivityApi extends BaseApi {
    /**
     * @param workspaceSid The SID of the Workspace with the Activity resources to fetch.
     * @param sid          The SID of the Activity resource to fetch.
     * @return Response from the API call
     */
    async fetchActivity(workspaceSid, sid, requestOptions) {
        const req = this.createRequest('GET');
        req.baseUrl('default4');
        const mapped = req.prepareArgs({
            workspaceSid: [workspaceSid, string()],
            sid: [sid, string()],
        });
        req.appendTemplatePath `/v1/Workspaces/${mapped.workspaceSid}/Activities/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(activitySchema, requestOptions);
    }
    /**
     * @param workspaceSid The SID of the Workspace with the Activity resources to update.
     * @param sid          The SID of the Activity resource to update.
     * @param friendlyName A descriptive string that you create to describe the Activity resource. It can be
     *                               up to 64 characters long. These names are used to calculate and expose statistics
     *                               about Workers, and provide visibility into the state of each Worker. Examples of
     *                               friendly names include: `on-call`, `break`, and `email`.
     * @return Response from the API call
     */
    async updateActivity(workspaceSid, sid, friendlyName, requestOptions) {
        const req = this.createRequest('POST');
        req.baseUrl('default4');
        const mapped = req.prepareArgs({
            workspaceSid: [workspaceSid, string()],
            sid: [sid, string()],
            friendlyName: [friendlyName, optional(string())],
        });
        req.header('Content-Type', 'application/x-www-form-urlencoded');
        req.form({ FriendlyName: mapped.friendlyName });
        req.appendTemplatePath `/v1/Workspaces/${mapped.workspaceSid}/Activities/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(activitySchema, requestOptions);
    }
    /**
     * @param workspaceSid The SID of the Workspace with the Activity resources to delete.
     * @param sid          The SID of the Activity resource to delete.
     * @return Response from the API call
     */
    async deleteActivity(workspaceSid, sid, requestOptions) {
        const req = this.createRequest('DELETE');
        req.baseUrl('default4');
        const mapped = req.prepareArgs({
            workspaceSid: [workspaceSid, string()],
            sid: [sid, string()],
        });
        req.appendTemplatePath `/v1/Workspaces/${mapped.workspaceSid}/Activities/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.call(requestOptions);
    }
    /**
     * @param workspaceSid The SID of the Workspace with the Activity resources to read.
     * @param friendlyName The `friendly_name` of the Activity resources to read.
     * @param available    Whether return only Activity resources that are available or unavailable. A value
     *                               of `true` returns only available activities. Values of '1' or `yes` also indicate
     *                               `true`. All other values represent `false` and return activities that are
     *                               unavailable.
     * @param pageSize     How many resources to return in each list page. The default is 50, and the maximum
     *                               is 1000.
     * @param page         The page index. This value is simply for client state.
     * @param pageToken    The page token. This is provided by the API.
     * @return Response from the API call
     */
    async listActivity(workspaceSid, friendlyName, available, pageSize, page, pageToken, requestOptions) {
        const req = this.createRequest('GET');
        req.baseUrl('default4');
        const mapped = req.prepareArgs({
            workspaceSid: [workspaceSid, string()],
            friendlyName: [friendlyName, optional(string())],
            available: [available, optional(string())],
            pageSize: [pageSize, optional(bigint())],
            page: [page, optional(number())],
            pageToken: [pageToken, optional(string())],
        });
        req.query('FriendlyName', mapped.friendlyName);
        req.query('Available', mapped.available);
        req.query('PageSize', mapped.pageSize);
        req.query('Page', mapped.page);
        req.query('PageToken', mapped.pageToken);
        req.appendTemplatePath `/v1/Workspaces/${mapped.workspaceSid}/Activities`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(listActivityResponseSchema, requestOptions);
    }
    /**
     * @param workspaceSid The SID of the Workspace that the new Activity belongs to.
     * @param friendlyName A descriptive string that you create to describe the Activity resource. It can be
     *                                up to 64 characters long. These names are used to calculate and expose statistics
     *                                about Workers, and provide visibility into the state of each Worker. Examples of
     *                                friendly names include: `on-call`, `break`, and `email`.
     * @param available    Whether the Worker should be eligible to receive a Task when it occupies the
     *                                Activity. A value of `true`, `1`, or `yes` specifies the Activity is available. All
     *                                other values specify that it is not. The value cannot be changed after the Activity
     *                                is created.
     * @return Response from the API call
     */
    async createActivity(workspaceSid, friendlyName, available, requestOptions) {
        const req = this.createRequest('POST');
        req.baseUrl('default4');
        const mapped = req.prepareArgs({
            workspaceSid: [workspaceSid, string()],
            friendlyName: [friendlyName, string()],
            available: [available, optional(boolean())],
        });
        req.header('Content-Type', 'application/x-www-form-urlencoded');
        req.form({
            FriendlyName: mapped.friendlyName,
            Available: mapped.available,
        });
        req.appendTemplatePath `/v1/Workspaces/${mapped.workspaceSid}/Activities`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(activitySchema, requestOptions);
    }
}
//# sourceMappingURL=taskrouterV1ActivityApi.js.map