/**
 * Twilio APIsLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */
import { configurationWebhookSchema, } from '../models/configurationWebhook.js';
import { configurationWebhookTargetSchema, } from '../models/configurationWebhookTarget.js';
import { conversationScopedWebhookSchema, } from '../models/conversationScopedWebhook.js';
import { conversationScopedWebhookMethodSchema, } from '../models/conversationScopedWebhookMethod.js';
import { conversationScopedWebhookTargetSchema, } from '../models/conversationScopedWebhookTarget.js';
import { listConversationScopedWebhookResponseSchema, } from '../models/listConversationScopedWebhookResponse.js';
import { listServiceConversationScopedWebhookResponseSchema, } from '../models/listServiceConversationScopedWebhookResponse.js';
import { serviceConversationScopedWebhookSchema, } from '../models/serviceConversationScopedWebhook.js';
import { serviceConversationScopedWebhookMethodSchema, } from '../models/serviceConversationScopedWebhookMethod.js';
import { serviceConversationScopedWebhookTargetSchema, } from '../models/serviceConversationScopedWebhookTarget.js';
import { serviceWebhookConfigurationSchema, } from '../models/serviceWebhookConfiguration.js';
import { array, bigint, number, optional, string } from '../schema.js';
import { BaseApi } from './baseApi.js';
export class ConversationsV1WebhookApi extends BaseApi {
    /**
     * A Webhook resource manages a service-level set of callback URLs and their configuration for
     * receiving all conversation events.
     *
     * @return Response from the API call
     */
    async fetchConfigurationWebhook(requestOptions) {
        const req = this.createRequest('GET', '/v1/Configuration/Webhooks');
        req.baseUrl('default2');
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(configurationWebhookSchema, requestOptions);
    }
    /**
     * A Webhook resource manages a service-level set of callback URLs and their configuration for
     * receiving all conversation events.
     *
     * @param method         The HTTP method to be used when sending a webhook request.
     * @param filters        The list of webhook event triggers that are enabled for this
     *                                                     Service: `onMessageAdded`, `onMessageUpdated`,
     *                                                     `onMessageRemoved`, `onMessageAdd`, `onMessageUpdate`,
     *                                                     `onMessageRemove`, `onConversationUpdated`,
     *                                                     `onConversationRemoved`, `onConversationAdd`,
     *                                                     `onConversationAdded`, `onConversationRemove`,
     *                                                     `onConversationUpdate`, `onConversationStateUpdated`,
     *                                                     `onParticipantAdded`, `onParticipantUpdated`,
     *                                                     `onParticipantRemoved`, `onParticipantAdd`,
     *                                                     `onParticipantRemove`, `onParticipantUpdate`,
     *                                                     `onDeliveryUpdated`, `onUserAdded`, `onUserUpdate`,
     *                                                     `onUserUpdated`
     * @param preWebhookUrl  The absolute url the pre-event webhook request should be sent
     *                                                     to.
     * @param postWebhookUrl The absolute url the post-event webhook request should be
     *                                                     sent to.
     * @param target         The routing target of the webhook. Can be ordinary or route
     *                                                     internally to Flex
     * @return Response from the API call
     */
    async updateConfigurationWebhook(method, filters, preWebhookUrl, postWebhookUrl, target, requestOptions) {
        const req = this.createRequest('POST', '/v1/Configuration/Webhooks');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            method: [method, optional(string())],
            filters: [filters, optional(array(string()))],
            preWebhookUrl: [preWebhookUrl, optional(string())],
            postWebhookUrl: [postWebhookUrl, optional(string())],
            target: [target, optional(configurationWebhookTargetSchema)],
        });
        req.header('Content-Type', 'application/x-www-form-urlencoded');
        req.form({
            Method: mapped.method,
            Filters: mapped.filters,
            PreWebhookUrl: mapped.preWebhookUrl,
            PostWebhookUrl: mapped.postWebhookUrl,
            Target: mapped.target,
        });
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(configurationWebhookSchema, requestOptions);
    }
    /**
     * Retrieve a list of all webhooks scoped to the conversation
     *
     * @param conversationSid The unique ID of the [Conversation](https://www.twilio.
     *                                  com/docs/conversations/api/conversation-resource) for this webhook.
     * @param pageSize        How many resources to return in each list page. The default is 5, and the
     *                                  maximum is 5.
     * @param page            The page index. This value is simply for client state.
     * @param pageToken       The page token. This is provided by the API.
     * @return Response from the API call
     */
    async listConversationScopedWebhook(conversationSid, pageSize, page, pageToken, requestOptions) {
        const req = this.createRequest('GET');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            conversationSid: [conversationSid, string()],
            pageSize: [pageSize, optional(bigint())],
            page: [page, optional(number())],
            pageToken: [pageToken, optional(string())],
        });
        req.query('PageSize', mapped.pageSize);
        req.query('Page', mapped.page);
        req.query('PageToken', mapped.pageToken);
        req.appendTemplatePath `/v1/Conversations/${mapped.conversationSid}/Webhooks`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(listConversationScopedWebhookResponseSchema, requestOptions);
    }
    /**
     * Create a new webhook scoped to the conversation
     *
     * @param conversationSid           The unique ID of the [Conversation](https:
     *                                                                     //www.twilio.
     *                                                                     com/docs/conversations/api/conversation-
     *                                                                     resource) for this webhook.
     * @param target                    The target of this webhook: `webhook`,
     *                                                                     `studio`, `trigger`
     * @param configurationUrl          The absolute url the webhook request should
     *                                                                     be sent to.
     * @param configurationMethod
     * @param configurationFilters      The list of events, firing webhook event for
     *                                                                     this Conversation.
     * @param configurationTriggers     The list of keywords, firing webhook event
     *                                                                     for this Conversation.
     * @param configurationFlowSid      The studio flow SID, where the webhook should
     *                                                                     be sent to.
     * @param configurationReplayAfter  The message index for which and it's
     *                                                                     successors the webhook will be replayed. Not
     *                                                                     set by default
     * @return Response from the API call
     */
    async createConversationScopedWebhook(conversationSid, target, configurationUrl, configurationMethod, configurationFilters, configurationTriggers, configurationFlowSid, configurationReplayAfter, requestOptions) {
        const req = this.createRequest('POST');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            conversationSid: [conversationSid, string()],
            target: [target, conversationScopedWebhookTargetSchema],
            configurationUrl: [configurationUrl, optional(string())],
            configurationMethod: [
                configurationMethod,
                optional(conversationScopedWebhookMethodSchema),
            ],
            configurationFilters: [configurationFilters, optional(array(string()))],
            configurationTriggers: [configurationTriggers, optional(array(string()))],
            configurationFlowSid: [configurationFlowSid, optional(string())],
            configurationReplayAfter: [configurationReplayAfter, optional(number())],
        });
        req.header('Content-Type', 'application/x-www-form-urlencoded');
        req.form({
            Target: mapped.target,
            'Configuration.Url': mapped.configurationUrl,
            'Configuration.Method': mapped.configurationMethod,
            'Configuration.Filters': mapped.configurationFilters,
            'Configuration.Triggers': mapped.configurationTriggers,
            'Configuration.FlowSid': mapped.configurationFlowSid,
            'Configuration.ReplayAfter': mapped.configurationReplayAfter,
        });
        req.appendTemplatePath `/v1/Conversations/${mapped.conversationSid}/Webhooks`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(conversationScopedWebhookSchema, requestOptions);
    }
    /**
     * Fetch the configuration of a conversation-scoped webhook
     *
     * @param conversationSid The unique ID of the [Conversation](https://www.twilio.
     *                                  com/docs/conversations/api/conversation-resource) for this webhook.
     * @param sid             A 34 character string that uniquely identifies this resource.
     * @return Response from the API call
     */
    async fetchConversationScopedWebhook(conversationSid, sid, requestOptions) {
        const req = this.createRequest('GET');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            conversationSid: [conversationSid, string()],
            sid: [sid, string()],
        });
        req.appendTemplatePath `/v1/Conversations/${mapped.conversationSid}/Webhooks/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(conversationScopedWebhookSchema, requestOptions);
    }
    /**
     * Update an existing conversation-scoped webhook
     *
     * @param conversationSid        The unique ID of the [Conversation](https://www.
     *                                                                  twilio.com/docs/conversations/api/conversation-
     *                                                                  resource) for this webhook.
     * @param sid                    A 34 character string that uniquely identifies
     *                                                                  this resource.
     * @param configurationUrl       The absolute url the webhook request should be
     *                                                                  sent to.
     * @param configurationMethod
     * @param configurationFilters   The list of events, firing webhook event for
     *                                                                  this Conversation.
     * @param configurationTriggers  The list of keywords, firing webhook event for
     *                                                                  this Conversation.
     * @param configurationFlowSid   The studio flow SID, where the webhook should be
     *                                                                  sent to.
     * @return Response from the API call
     */
    async updateConversationScopedWebhook(conversationSid, sid, configurationUrl, configurationMethod, configurationFilters, configurationTriggers, configurationFlowSid, requestOptions) {
        const req = this.createRequest('POST');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            conversationSid: [conversationSid, string()],
            sid: [sid, string()],
            configurationUrl: [configurationUrl, optional(string())],
            configurationMethod: [
                configurationMethod,
                optional(conversationScopedWebhookMethodSchema),
            ],
            configurationFilters: [configurationFilters, optional(array(string()))],
            configurationTriggers: [configurationTriggers, optional(array(string()))],
            configurationFlowSid: [configurationFlowSid, optional(string())],
        });
        req.header('Content-Type', 'application/x-www-form-urlencoded');
        req.form({
            'Configuration.Url': mapped.configurationUrl,
            'Configuration.Method': mapped.configurationMethod,
            'Configuration.Filters': mapped.configurationFilters,
            'Configuration.Triggers': mapped.configurationTriggers,
            'Configuration.FlowSid': mapped.configurationFlowSid,
        });
        req.appendTemplatePath `/v1/Conversations/${mapped.conversationSid}/Webhooks/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(conversationScopedWebhookSchema, requestOptions);
    }
    /**
     * Remove an existing webhook scoped to the conversation
     *
     * @param conversationSid The unique ID of the [Conversation](https://www.twilio.
     *                                  com/docs/conversations/api/conversation-resource) for this webhook.
     * @param sid             A 34 character string that uniquely identifies this resource.
     * @return Response from the API call
     */
    async deleteConversationScopedWebhook(conversationSid, sid, requestOptions) {
        const req = this.createRequest('DELETE');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            conversationSid: [conversationSid, string()],
            sid: [sid, string()],
        });
        req.appendTemplatePath `/v1/Conversations/${mapped.conversationSid}/Webhooks/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.call(requestOptions);
    }
    /**
     * Create a new webhook scoped to the conversation in a specific service
     *
     * @param chatServiceSid            The SID of the [Conversation
     *                                                                            Service](https://www.twilio.
     *                                                                            com/docs/conversations/api/service-
     *                                                                            resource) the Participant resource is
     *                                                                            associated with.
     * @param conversationSid           The unique ID of the
     *                                                                            [Conversation](https://www.twilio.
     *                                                                            com/docs/conversations/api/conversation-
     *                                                                            resource) for this webhook.
     * @param target                    The target of this webhook: `webhook`,
     *                                                                            `studio`, `trigger`
     * @param configurationUrl          The absolute url the webhook request
     *                                                                            should be sent to.
     * @param configurationMethod
     * @param configurationFilters      The list of events, firing webhook
     *                                                                            event for this Conversation.
     * @param configurationTriggers     The list of keywords, firing webhook
     *                                                                            event for this Conversation.
     * @param configurationFlowSid      The studio flow SID, where the webhook
     *                                                                            should be sent to.
     * @param configurationReplayAfter  The message index for which and it's
     *                                                                            successors the webhook will be replayed.
     *                                                                            Not set by default
     * @return Response from the API call
     */
    async createServiceConversationScopedWebhook(chatServiceSid, conversationSid, target, configurationUrl, configurationMethod, configurationFilters, configurationTriggers, configurationFlowSid, configurationReplayAfter, requestOptions) {
        const req = this.createRequest('POST');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            chatServiceSid: [chatServiceSid, string()],
            conversationSid: [conversationSid, string()],
            target: [target, serviceConversationScopedWebhookTargetSchema],
            configurationUrl: [configurationUrl, optional(string())],
            configurationMethod: [
                configurationMethod,
                optional(serviceConversationScopedWebhookMethodSchema),
            ],
            configurationFilters: [configurationFilters, optional(array(string()))],
            configurationTriggers: [configurationTriggers, optional(array(string()))],
            configurationFlowSid: [configurationFlowSid, optional(string())],
            configurationReplayAfter: [configurationReplayAfter, optional(number())],
        });
        req.header('Content-Type', 'application/x-www-form-urlencoded');
        req.form({
            Target: mapped.target,
            'Configuration.Url': mapped.configurationUrl,
            'Configuration.Method': mapped.configurationMethod,
            'Configuration.Filters': mapped.configurationFilters,
            'Configuration.Triggers': mapped.configurationTriggers,
            'Configuration.FlowSid': mapped.configurationFlowSid,
            'Configuration.ReplayAfter': mapped.configurationReplayAfter,
        });
        req.appendTemplatePath `/v1/Services/${mapped.chatServiceSid}/Conversations/${mapped.conversationSid}/Webhooks`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(serviceConversationScopedWebhookSchema, requestOptions);
    }
    /**
     * Retrieve a list of all webhooks scoped to the conversation
     *
     * @param chatServiceSid  The SID of the [Conversation Service](https://www.twilio.
     *                                  com/docs/conversations/api/service-resource) the Participant resource is
     *                                  associated with.
     * @param conversationSid The unique ID of the [Conversation](https://www.twilio.
     *                                  com/docs/conversations/api/conversation-resource) for this webhook.
     * @param pageSize        How many resources to return in each list page. The default is 5, and the
     *                                  maximum is 5.
     * @param page            The page index. This value is simply for client state.
     * @param pageToken       The page token. This is provided by the API.
     * @return Response from the API call
     */
    async listServiceConversationScopedWebhook(chatServiceSid, conversationSid, pageSize, page, pageToken, requestOptions) {
        const req = this.createRequest('GET');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            chatServiceSid: [chatServiceSid, string()],
            conversationSid: [conversationSid, string()],
            pageSize: [pageSize, optional(bigint())],
            page: [page, optional(number())],
            pageToken: [pageToken, optional(string())],
        });
        req.query('PageSize', mapped.pageSize);
        req.query('Page', mapped.page);
        req.query('PageToken', mapped.pageToken);
        req.appendTemplatePath `/v1/Services/${mapped.chatServiceSid}/Conversations/${mapped.conversationSid}/Webhooks`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(listServiceConversationScopedWebhookResponseSchema, requestOptions);
    }
    /**
     * Update an existing conversation-scoped webhook
     *
     * @param chatServiceSid         The SID of the [Conversation
     *                                                                         Service](https://www.twilio.
     *                                                                         com/docs/conversations/api/service-
     *                                                                         resource) the Participant resource is
     *                                                                         associated with.
     * @param conversationSid        The unique ID of the [Conversation](https:
     *                                                                         //www.twilio.
     *                                                                         com/docs/conversations/api/conversation-
     *                                                                         resource) for this webhook.
     * @param sid                    A 34 character string that uniquely
     *                                                                         identifies this resource.
     * @param configurationUrl       The absolute url the webhook request
     *                                                                         should be sent to.
     * @param configurationMethod
     * @param configurationFilters   The list of events, firing webhook event
     *                                                                         for this Conversation.
     * @param configurationTriggers  The list of keywords, firing webhook
     *                                                                         event for this Conversation.
     * @param configurationFlowSid   The studio flow SID, where the webhook
     *                                                                         should be sent to.
     * @return Response from the API call
     */
    async updateServiceConversationScopedWebhook(chatServiceSid, conversationSid, sid, configurationUrl, configurationMethod, configurationFilters, configurationTriggers, configurationFlowSid, requestOptions) {
        const req = this.createRequest('POST');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            chatServiceSid: [chatServiceSid, string()],
            conversationSid: [conversationSid, string()],
            sid: [sid, string()],
            configurationUrl: [configurationUrl, optional(string())],
            configurationMethod: [
                configurationMethod,
                optional(serviceConversationScopedWebhookMethodSchema),
            ],
            configurationFilters: [configurationFilters, optional(array(string()))],
            configurationTriggers: [configurationTriggers, optional(array(string()))],
            configurationFlowSid: [configurationFlowSid, optional(string())],
        });
        req.header('Content-Type', 'application/x-www-form-urlencoded');
        req.form({
            'Configuration.Url': mapped.configurationUrl,
            'Configuration.Method': mapped.configurationMethod,
            'Configuration.Filters': mapped.configurationFilters,
            'Configuration.Triggers': mapped.configurationTriggers,
            'Configuration.FlowSid': mapped.configurationFlowSid,
        });
        req.appendTemplatePath `/v1/Services/${mapped.chatServiceSid}/Conversations/${mapped.conversationSid}/Webhooks/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(serviceConversationScopedWebhookSchema, requestOptions);
    }
    /**
     * Remove an existing webhook scoped to the conversation
     *
     * @param chatServiceSid  The SID of the [Conversation Service](https://www.twilio.
     *                                  com/docs/conversations/api/service-resource) the Participant resource is
     *                                  associated with.
     * @param conversationSid The unique ID of the [Conversation](https://www.twilio.
     *                                  com/docs/conversations/api/conversation-resource) for this webhook.
     * @param sid             A 34 character string that uniquely identifies this resource.
     * @return Response from the API call
     */
    async deleteServiceConversationScopedWebhook(chatServiceSid, conversationSid, sid, requestOptions) {
        const req = this.createRequest('DELETE');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            chatServiceSid: [chatServiceSid, string()],
            conversationSid: [conversationSid, string()],
            sid: [sid, string()],
        });
        req.appendTemplatePath `/v1/Services/${mapped.chatServiceSid}/Conversations/${mapped.conversationSid}/Webhooks/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.call(requestOptions);
    }
    /**
     * Fetch the configuration of a conversation-scoped webhook
     *
     * @param chatServiceSid  The SID of the [Conversation Service](https://www.twilio.
     *                                  com/docs/conversations/api/service-resource) the Participant resource is
     *                                  associated with.
     * @param conversationSid The unique ID of the [Conversation](https://www.twilio.
     *                                  com/docs/conversations/api/conversation-resource) for this webhook.
     * @param sid             A 34 character string that uniquely identifies this resource.
     * @return Response from the API call
     */
    async fetchServiceConversationScopedWebhook(chatServiceSid, conversationSid, sid, requestOptions) {
        const req = this.createRequest('GET');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            chatServiceSid: [chatServiceSid, string()],
            conversationSid: [conversationSid, string()],
            sid: [sid, string()],
        });
        req.appendTemplatePath `/v1/Services/${mapped.chatServiceSid}/Conversations/${mapped.conversationSid}/Webhooks/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(serviceConversationScopedWebhookSchema, requestOptions);
    }
    /**
     * Update a specific Webhook.
     *
     * @param chatServiceSid The unique ID of the [Conversation Service](https://www.twilio.
     *                                   com/docs/conversations/api/service-resource) this conversation belongs to.
     * @param preWebhookUrl  The absolute url the pre-event webhook request should be sent to.
     * @param postWebhookUrl The absolute url the post-event webhook request should be sent to.
     * @param filters        The list of events that your configured webhook targets will receive. Events
     *                                   not configured here will not fire. Possible values are `onParticipantAdd`,
     *                                   `onParticipantAdded`, `onDeliveryUpdated`, `onConversationUpdated`,
     *                                   `onConversationRemove`, `onParticipantRemove`, `onConversationUpdate`,
     *                                   `onMessageAdd`, `onMessageRemoved`, `onParticipantUpdated`,
     *                                   `onConversationAdded`, `onMessageAdded`, `onConversationAdd`,
     *                                   `onConversationRemoved`, `onParticipantUpdate`, `onMessageRemove`,
     *                                   `onMessageUpdated`, `onParticipantRemoved`, `onMessageUpdate` or
     *                                   `onConversationStateUpdated`.
     * @param method         The HTTP method to be used when sending a webhook request. One of `GET` or
     *                                   `POST`.
     * @return Response from the API call
     */
    async updateServiceWebhookConfiguration(chatServiceSid, preWebhookUrl, postWebhookUrl, filters, method, requestOptions) {
        const req = this.createRequest('POST');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            chatServiceSid: [chatServiceSid, string()],
            preWebhookUrl: [preWebhookUrl, optional(string())],
            postWebhookUrl: [postWebhookUrl, optional(string())],
            filters: [filters, optional(array(string()))],
            method: [method, optional(string())],
        });
        req.header('Content-Type', 'application/x-www-form-urlencoded');
        req.form({
            PreWebhookUrl: mapped.preWebhookUrl,
            PostWebhookUrl: mapped.postWebhookUrl,
            Filters: mapped.filters,
            Method: mapped.method,
        });
        req.appendTemplatePath `/v1/Services/${mapped.chatServiceSid}/Configuration/Webhooks`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(serviceWebhookConfigurationSchema, requestOptions);
    }
    /**
     * Fetch a specific service webhook configuration.
     *
     * @param chatServiceSid The unique ID of the [Conversation Service](https://www.twilio.
     *                                 com/docs/conversations/api/service-resource) this conversation belongs to.
     * @return Response from the API call
     */
    async fetchServiceWebhookConfiguration(chatServiceSid, requestOptions) {
        const req = this.createRequest('GET');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            chatServiceSid: [chatServiceSid, string()],
        });
        req.appendTemplatePath `/v1/Services/${mapped.chatServiceSid}/Configuration/Webhooks`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(serviceWebhookConfigurationSchema, requestOptions);
    }
}
//# sourceMappingURL=conversationsV1WebhookApi.js.map