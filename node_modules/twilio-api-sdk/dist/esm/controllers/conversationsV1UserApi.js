/**
 * Twilio APIsLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */
import { channelWebhookEnabledTypeSchema, } from '../models/channelWebhookEnabledType.js';
import { listServiceUserResponseSchema, } from '../models/listServiceUserResponse.js';
import { listUserResponseSchema, } from '../models/listUserResponse.js';
import { serviceUserSchema } from '../models/serviceUser.js';
import { userSchema } from '../models/user.js';
import { bigint, number, optional, string } from '../schema.js';
import { BaseApi } from './baseApi.js';
export class ConversationsV1UserApi extends BaseApi {
    /**
     * Add a new conversation user to your service
     *
     * @param chatServiceSid           The SID of the [Conversation Service](https://www.
     *                                                              twilio.com/docs/conversations/api/service-resource)
     *                                                              the User resource is associated with.
     * @param identity                 The application-defined string that uniquely
     *                                                              identifies the resource's User within the
     *                                                              [Conversation Service](https://www.twilio.
     *                                                              com/docs/conversations/api/service-resource). This
     *                                                              value is often a username or an email address, and is
     *                                                              case-sensitive.
     * @param xTwilioWebhookEnabled    The X-Twilio-Webhook-Enabled HTTP request header
     * @param friendlyName             The string that you assigned to describe the
     *                                                              resource.
     * @param attributes               The JSON Object string that stores application-
     *                                                              specific data. If attributes have not been set, `{}`
     *                                                              is returned.
     * @param roleSid                  The SID of a service-level [Role](https://www.twilio.
     *                                                              com/docs/conversations/api/role-resource) to assign
     *                                                              to the user.
     * @return Response from the API call
     */
    async createServiceUser(chatServiceSid, identity, xTwilioWebhookEnabled, friendlyName, attributes, roleSid, requestOptions) {
        const req = this.createRequest('POST');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            chatServiceSid: [chatServiceSid, string()],
            identity: [identity, string()],
            xTwilioWebhookEnabled: [
                xTwilioWebhookEnabled,
                optional(channelWebhookEnabledTypeSchema),
            ],
            friendlyName: [friendlyName, optional(string())],
            attributes: [attributes, optional(string())],
            roleSid: [roleSid, optional(string())],
        });
        req.header('Content-Type', 'application/x-www-form-urlencoded');
        req.header('X-Twilio-Webhook-Enabled', mapped.xTwilioWebhookEnabled);
        req.form({
            Identity: mapped.identity,
            FriendlyName: mapped.friendlyName,
            Attributes: mapped.attributes,
            RoleSid: mapped.roleSid,
        });
        req.appendTemplatePath `/v1/Services/${mapped.chatServiceSid}/Users`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(serviceUserSchema, requestOptions);
    }
    /**
     * Retrieve a list of all conversation users in your service
     *
     * @param chatServiceSid The SID of the [Conversation Service](https://www.twilio.
     *                                 com/docs/conversations/api/service-resource) to read the User resources from.
     * @param pageSize       How many resources to return in each list page. The default is 50, and the
     *                                 maximum is 50.
     * @param page           The page index. This value is simply for client state.
     * @param pageToken      The page token. This is provided by the API.
     * @return Response from the API call
     */
    async listServiceUser(chatServiceSid, pageSize, page, pageToken, requestOptions) {
        const req = this.createRequest('GET');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            chatServiceSid: [chatServiceSid, string()],
            pageSize: [pageSize, optional(bigint())],
            page: [page, optional(number())],
            pageToken: [pageToken, optional(string())],
        });
        req.query('PageSize', mapped.pageSize);
        req.query('Page', mapped.page);
        req.query('PageToken', mapped.pageToken);
        req.appendTemplatePath `/v1/Services/${mapped.chatServiceSid}/Users`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(listServiceUserResponseSchema, requestOptions);
    }
    /**
     * Update an existing conversation user in your service
     *
     * @param chatServiceSid           The SID of the [Conversation Service](https://www.
     *                                                              twilio.com/docs/conversations/api/service-resource)
     *                                                              the User resource is associated with.
     * @param sid                      The SID of the User resource to update. This value
     *                                                              can be either the `sid` or the `identity` of the User
     *                                                              resource to update.
     * @param xTwilioWebhookEnabled    The X-Twilio-Webhook-Enabled HTTP request header
     * @param friendlyName             The string that you assigned to describe the
     *                                                              resource.
     * @param attributes               The JSON Object string that stores application-
     *                                                              specific data. If attributes have not been set, `{}`
     *                                                              is returned.
     * @param roleSid                  The SID of a service-level [Role](https://www.twilio.
     *                                                              com/docs/conversations/api/role-resource) to assign
     *                                                              to the user.
     * @return Response from the API call
     */
    async updateServiceUser(chatServiceSid, sid, xTwilioWebhookEnabled, friendlyName, attributes, roleSid, requestOptions) {
        const req = this.createRequest('POST');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            chatServiceSid: [chatServiceSid, string()],
            sid: [sid, string()],
            xTwilioWebhookEnabled: [
                xTwilioWebhookEnabled,
                optional(channelWebhookEnabledTypeSchema),
            ],
            friendlyName: [friendlyName, optional(string())],
            attributes: [attributes, optional(string())],
            roleSid: [roleSid, optional(string())],
        });
        req.header('Content-Type', 'application/x-www-form-urlencoded');
        req.header('X-Twilio-Webhook-Enabled', mapped.xTwilioWebhookEnabled);
        req.form({
            FriendlyName: mapped.friendlyName,
            Attributes: mapped.attributes,
            RoleSid: mapped.roleSid,
        });
        req.appendTemplatePath `/v1/Services/${mapped.chatServiceSid}/Users/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(serviceUserSchema, requestOptions);
    }
    /**
     * Remove a conversation user from your service
     *
     * @param chatServiceSid           The SID of the [Conversation Service](https://www.
     *                                                              twilio.com/docs/conversations/api/service-resource)
     *                                                              to delete the User resource from.
     * @param sid                      The SID of the User resource to delete. This value
     *                                                              can be either the `sid` or the `identity` of the User
     *                                                              resource to delete.
     * @param xTwilioWebhookEnabled    The X-Twilio-Webhook-Enabled HTTP request header
     * @return Response from the API call
     */
    async deleteServiceUser(chatServiceSid, sid, xTwilioWebhookEnabled, requestOptions) {
        const req = this.createRequest('DELETE');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            chatServiceSid: [chatServiceSid, string()],
            sid: [sid, string()],
            xTwilioWebhookEnabled: [
                xTwilioWebhookEnabled,
                optional(channelWebhookEnabledTypeSchema),
            ],
        });
        req.header('X-Twilio-Webhook-Enabled', mapped.xTwilioWebhookEnabled);
        req.appendTemplatePath `/v1/Services/${mapped.chatServiceSid}/Users/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.call(requestOptions);
    }
    /**
     * Fetch a conversation user from your service
     *
     * @param chatServiceSid The SID of the [Conversation Service](https://www.twilio.
     *                                 com/docs/conversations/api/service-resource) to fetch the User resource from.
     * @param sid            The SID of the User resource to fetch. This value can be either the `sid` or the
     *                                 `identity` of the User resource to fetch.
     * @return Response from the API call
     */
    async fetchServiceUser(chatServiceSid, sid, requestOptions) {
        const req = this.createRequest('GET');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            chatServiceSid: [chatServiceSid, string()],
            sid: [sid, string()],
        });
        req.appendTemplatePath `/v1/Services/${mapped.chatServiceSid}/Users/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(serviceUserSchema, requestOptions);
    }
    /**
     * Add a new conversation user to your account's default service
     *
     * @param identity                 The application-defined string that uniquely
     *                                                              identifies the resource's User within the
     *                                                              [Conversation Service](https://www.twilio.
     *                                                              com/docs/conversations/api/service-resource). This
     *                                                              value is often a username or an email address, and is
     *                                                              case-sensitive.
     * @param xTwilioWebhookEnabled    The X-Twilio-Webhook-Enabled HTTP request header
     * @param friendlyName             The string that you assigned to describe the
     *                                                              resource.
     * @param attributes               The JSON Object string that stores application-
     *                                                              specific data. If attributes have not been set, `{}`
     *                                                              is returned.
     * @param roleSid                  The SID of a service-level [Role](https://www.twilio.
     *                                                              com/docs/conversations/api/role-resource) to assign
     *                                                              to the user.
     * @return Response from the API call
     */
    async createUser(identity, xTwilioWebhookEnabled, friendlyName, attributes, roleSid, requestOptions) {
        const req = this.createRequest('POST', '/v1/Users');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            identity: [identity, string()],
            xTwilioWebhookEnabled: [
                xTwilioWebhookEnabled,
                optional(channelWebhookEnabledTypeSchema),
            ],
            friendlyName: [friendlyName, optional(string())],
            attributes: [attributes, optional(string())],
            roleSid: [roleSid, optional(string())],
        });
        req.header('Content-Type', 'application/x-www-form-urlencoded');
        req.header('X-Twilio-Webhook-Enabled', mapped.xTwilioWebhookEnabled);
        req.form({
            Identity: mapped.identity,
            FriendlyName: mapped.friendlyName,
            Attributes: mapped.attributes,
            RoleSid: mapped.roleSid,
        });
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(userSchema, requestOptions);
    }
    /**
     * Retrieve a list of all conversation users in your account's default service
     *
     * @param pageSize  How many resources to return in each list page. The default is 50, and the maximum is
     *                            50.
     * @param page      The page index. This value is simply for client state.
     * @param pageToken The page token. This is provided by the API.
     * @return Response from the API call
     */
    async listUser(pageSize, page, pageToken, requestOptions) {
        const req = this.createRequest('GET', '/v1/Users');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            pageSize: [pageSize, optional(bigint())],
            page: [page, optional(number())],
            pageToken: [pageToken, optional(string())],
        });
        req.query('PageSize', mapped.pageSize);
        req.query('Page', mapped.page);
        req.query('PageToken', mapped.pageToken);
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(listUserResponseSchema, requestOptions);
    }
    /**
     * Update an existing conversation user in your account's default service
     *
     * @param sid                      The SID of the User resource to update. This value
     *                                                              can be either the `sid` or the `identity` of the User
     *                                                              resource to update.
     * @param xTwilioWebhookEnabled    The X-Twilio-Webhook-Enabled HTTP request header
     * @param friendlyName             The string that you assigned to describe the
     *                                                              resource.
     * @param attributes               The JSON Object string that stores application-
     *                                                              specific data. If attributes have not been set, `{}`
     *                                                              is returned.
     * @param roleSid                  The SID of a service-level [Role](https://www.twilio.
     *                                                              com/docs/conversations/api/role-resource) to assign
     *                                                              to the user.
     * @return Response from the API call
     */
    async updateUser(sid, xTwilioWebhookEnabled, friendlyName, attributes, roleSid, requestOptions) {
        const req = this.createRequest('POST');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            sid: [sid, string()],
            xTwilioWebhookEnabled: [
                xTwilioWebhookEnabled,
                optional(channelWebhookEnabledTypeSchema),
            ],
            friendlyName: [friendlyName, optional(string())],
            attributes: [attributes, optional(string())],
            roleSid: [roleSid, optional(string())],
        });
        req.header('Content-Type', 'application/x-www-form-urlencoded');
        req.header('X-Twilio-Webhook-Enabled', mapped.xTwilioWebhookEnabled);
        req.form({
            FriendlyName: mapped.friendlyName,
            Attributes: mapped.attributes,
            RoleSid: mapped.roleSid,
        });
        req.appendTemplatePath `/v1/Users/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(userSchema, requestOptions);
    }
    /**
     * Remove a conversation user from your account's default service
     *
     * @param sid                      The SID of the User resource to delete. This value
     *                                                              can be either the `sid` or the `identity` of the User
     *                                                              resource to delete.
     * @param xTwilioWebhookEnabled    The X-Twilio-Webhook-Enabled HTTP request header
     * @return Response from the API call
     */
    async deleteUser(sid, xTwilioWebhookEnabled, requestOptions) {
        const req = this.createRequest('DELETE');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            sid: [sid, string()],
            xTwilioWebhookEnabled: [
                xTwilioWebhookEnabled,
                optional(channelWebhookEnabledTypeSchema),
            ],
        });
        req.header('X-Twilio-Webhook-Enabled', mapped.xTwilioWebhookEnabled);
        req.appendTemplatePath `/v1/Users/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.call(requestOptions);
    }
    /**
     * Fetch a conversation user from your account's default service
     *
     * @param sid The SID of the User resource to fetch. This value can be either the `sid` or the `identity`
     *                      of the User resource to fetch.
     * @return Response from the API call
     */
    async fetchUser(sid, requestOptions) {
        const req = this.createRequest('GET');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({ sid: [sid, string()] });
        req.appendTemplatePath `/v1/Users/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(userSchema, requestOptions);
    }
}
//# sourceMappingURL=conversationsV1UserApi.js.map