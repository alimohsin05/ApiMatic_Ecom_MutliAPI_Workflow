/**
 * Twilio APIsLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */
import { eventSchema } from '../models/event.js';
import { listEventResponseSchema, } from '../models/listEventResponse.js';
import { bigint, number, optional, string } from '../schema.js';
import { BaseApi } from './baseApi.js';
export class TaskrouterV1EventApi extends BaseApi {
    /**
     * @param workspaceSid The SID of the Workspace with the Event to fetch.
     * @param sid          The SID of the Event resource to fetch.
     * @return Response from the API call
     */
    async fetchEvent(workspaceSid, sid, requestOptions) {
        const req = this.createRequest('GET');
        req.baseUrl('default4');
        const mapped = req.prepareArgs({
            workspaceSid: [workspaceSid, string()],
            sid: [sid, string()],
        });
        req.appendTemplatePath `/v1/Workspaces/${mapped.workspaceSid}/Events/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(eventSchema, requestOptions);
    }
    /**
     * @param workspaceSid   The SID of the Workspace with the Events to read. Returns only the Events that
     *                                 pertain to the specified Workspace.
     * @param endDate        Only include Events that occurred on or before this date, specified in GMT as an
     *                                 [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date-time.
     * @param eventType      The type of Events to read. Returns only Events of the type specified.
     * @param minutes        The period of events to read in minutes. Returns only Events that occurred since
     *                                 this many minutes in the past. The default is `15` minutes. Task Attributes for
     *                                 Events occuring more 43,200 minutes ago will be redacted.
     * @param reservationSid The SID of the Reservation with the Events to read. Returns only Events that
     *                                 pertain to the specified Reservation.
     * @param startDate      Only include Events from on or after this date and time, specified in [ISO
     *                                 8601](https://en.wikipedia.org/wiki/ISO_8601) format. Task Attributes for Events
     *                                 older than 30 days will be redacted.
     * @param taskQueueSid   The SID of the TaskQueue with the Events to read. Returns only the Events that
     *                                 pertain to the specified TaskQueue.
     * @param taskSid        The SID of the Task with the Events to read. Returns only the Events that pertain
     *                                 to the specified Task.
     * @param workerSid      The SID of the Worker with the Events to read. Returns only the Events that
     *                                 pertain to the specified Worker.
     * @param workflowSid    The SID of the Workflow with the Events to read. Returns only the Events that
     *                                 pertain to the specified Workflow.
     * @param taskChannel    The TaskChannel with the Events to read. Returns only the Events that pertain to
     *                                 the specified TaskChannel.
     * @param sid            The SID of the Event resource to read.
     * @param pageSize       How many resources to return in each list page. The default is 50, and the
     *                                 maximum is 1000.
     * @param page           The page index. This value is simply for client state.
     * @param pageToken      The page token. This is provided by the API.
     * @return Response from the API call
     */
    async listEvent(workspaceSid, endDate, eventType, minutes, reservationSid, startDate, taskQueueSid, taskSid, workerSid, workflowSid, taskChannel, sid, pageSize, page, pageToken, requestOptions) {
        const req = this.createRequest('GET');
        req.baseUrl('default4');
        const mapped = req.prepareArgs({
            workspaceSid: [workspaceSid, string()],
            endDate: [endDate, optional(string())],
            eventType: [eventType, optional(string())],
            minutes: [minutes, optional(number())],
            reservationSid: [reservationSid, optional(string())],
            startDate: [startDate, optional(string())],
            taskQueueSid: [taskQueueSid, optional(string())],
            taskSid: [taskSid, optional(string())],
            workerSid: [workerSid, optional(string())],
            workflowSid: [workflowSid, optional(string())],
            taskChannel: [taskChannel, optional(string())],
            sid: [sid, optional(string())],
            pageSize: [pageSize, optional(bigint())],
            page: [page, optional(number())],
            pageToken: [pageToken, optional(string())],
        });
        req.query('EndDate', mapped.endDate);
        req.query('EventType', mapped.eventType);
        req.query('Minutes', mapped.minutes);
        req.query('ReservationSid', mapped.reservationSid);
        req.query('StartDate', mapped.startDate);
        req.query('TaskQueueSid', mapped.taskQueueSid);
        req.query('TaskSid', mapped.taskSid);
        req.query('WorkerSid', mapped.workerSid);
        req.query('WorkflowSid', mapped.workflowSid);
        req.query('TaskChannel', mapped.taskChannel);
        req.query('Sid', mapped.sid);
        req.query('PageSize', mapped.pageSize);
        req.query('Page', mapped.page);
        req.query('PageToken', mapped.pageToken);
        req.appendTemplatePath `/v1/Workspaces/${mapped.workspaceSid}/Events`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(listEventResponseSchema, requestOptions);
    }
}
//# sourceMappingURL=taskrouterV1EventApi.js.map