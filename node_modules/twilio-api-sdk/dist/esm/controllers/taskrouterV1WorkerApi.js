/**
 * Twilio APIsLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */
import { listWorkerResponseSchema, } from '../models/listWorkerResponse.js';
import { workerSchema } from '../models/worker.js';
import { bigint, boolean, number, optional, string } from '../schema.js';
import { BaseApi } from './baseApi.js';
export class TaskrouterV1WorkerApi extends BaseApi {
    /**
     * @param workspaceSid            The SID of the Workspace with the Workers to read.
     * @param activityName            The `activity_name` of the Worker resources to read.
     * @param activitySid             The `activity_sid` of the Worker resources to read.
     * @param available               Whether to return only Worker resources that are available or
     *                                          unavailable. Can be `true`, `1`, or `yes` to return Worker resources that
     *                                          are available, and `false`, or any value returns the Worker resources
     *                                          that are not available.
     * @param friendlyName            The `friendly_name` of the Worker resources to read.
     * @param targetWorkersExpression Filter by Workers that would match an expression. In addition to fields
     *                                          in the workers' attributes, the expression can include the following
     *                                          worker fields: `sid`, `friendly_name`, `activity_sid`, or
     *                                          `activity_name`
     * @param taskQueueName           The `friendly_name` of the TaskQueue that the Workers to read are
     *                                          eligible for.
     * @param taskQueueSid            The SID of the TaskQueue that the Workers to read are eligible for.
     * @param ordering                Sorting parameter for Workers
     * @param pageSize                How many resources to return in each list page. The default is 50, and
     *                                          the maximum is 1000.
     * @param page                    The page index. This value is simply for client state.
     * @param pageToken               The page token. This is provided by the API.
     * @return Response from the API call
     */
    async listWorker(workspaceSid, activityName, activitySid, available, friendlyName, targetWorkersExpression, taskQueueName, taskQueueSid, ordering, pageSize, page, pageToken, requestOptions) {
        const req = this.createRequest('GET');
        req.baseUrl('default4');
        const mapped = req.prepareArgs({
            workspaceSid: [workspaceSid, string()],
            activityName: [activityName, optional(string())],
            activitySid: [activitySid, optional(string())],
            available: [available, optional(string())],
            friendlyName: [friendlyName, optional(string())],
            targetWorkersExpression: [targetWorkersExpression, optional(string())],
            taskQueueName: [taskQueueName, optional(string())],
            taskQueueSid: [taskQueueSid, optional(string())],
            ordering: [ordering, optional(string())],
            pageSize: [pageSize, optional(bigint())],
            page: [page, optional(number())],
            pageToken: [pageToken, optional(string())],
        });
        req.query('ActivityName', mapped.activityName);
        req.query('ActivitySid', mapped.activitySid);
        req.query('Available', mapped.available);
        req.query('FriendlyName', mapped.friendlyName);
        req.query('TargetWorkersExpression', mapped.targetWorkersExpression);
        req.query('TaskQueueName', mapped.taskQueueName);
        req.query('TaskQueueSid', mapped.taskQueueSid);
        req.query('Ordering', mapped.ordering);
        req.query('PageSize', mapped.pageSize);
        req.query('Page', mapped.page);
        req.query('PageToken', mapped.pageToken);
        req.appendTemplatePath `/v1/Workspaces/${mapped.workspaceSid}/Workers`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(listWorkerResponseSchema, requestOptions);
    }
    /**
     * @param workspaceSid The SID of the Workspace that the new Worker belongs to.
     * @param friendlyName A descriptive string that you create to describe the new Worker. It can be up to 64
     *                               characters long.
     * @param activitySid  The SID of a valid Activity that will describe the new Worker's initial state. See
     *                               [Activities](https://www.twilio.com/docs/taskrouter/api/activity) for more
     *                               information. If not provided, the new Worker's initial state is the
     *                               `default_activity_sid` configured on the Workspace.
     * @param attributes   A valid JSON string that describes the new Worker. For example: `{ "email":
     *                               "Bob@example.com", "phone": "+5095551234" }`. This data is passed to the
     *                               `assignment_callback_url` when TaskRouter assigns a Task to the Worker. Defaults to
     *                               {}.
     * @return Response from the API call
     */
    async createWorker(workspaceSid, friendlyName, activitySid, attributes, requestOptions) {
        const req = this.createRequest('POST');
        req.baseUrl('default4');
        const mapped = req.prepareArgs({
            workspaceSid: [workspaceSid, string()],
            friendlyName: [friendlyName, string()],
            activitySid: [activitySid, optional(string())],
            attributes: [attributes, optional(string())],
        });
        req.header('Content-Type', 'application/x-www-form-urlencoded');
        req.form({
            FriendlyName: mapped.friendlyName,
            ActivitySid: mapped.activitySid,
            Attributes: mapped.attributes,
        });
        req.appendTemplatePath `/v1/Workspaces/${mapped.workspaceSid}/Workers`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(workerSchema, requestOptions);
    }
    /**
     * @param workspaceSid The SID of the Workspace with the Worker to fetch.
     * @param sid          The SID of the Worker resource to fetch.
     * @return Response from the API call
     */
    async fetchWorker(workspaceSid, sid, requestOptions) {
        const req = this.createRequest('GET');
        req.baseUrl('default4');
        const mapped = req.prepareArgs({
            workspaceSid: [workspaceSid, string()],
            sid: [sid, string()],
        });
        req.appendTemplatePath `/v1/Workspaces/${mapped.workspaceSid}/Workers/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(workerSchema, requestOptions);
    }
    /**
     * @param workspaceSid              The SID of the Workspace with the Worker to update.
     * @param sid                       The SID of the Worker resource to update.
     * @param ifMatch                   The If-Match HTTP request header
     * @param activitySid               The SID of a valid Activity that will describe the Worker's initial
     *                                             state. See [Activities](https://www.twilio.
     *                                             com/docs/taskrouter/api/activity) for more information.
     * @param attributes                The JSON string that describes the Worker. For example: `{ "email":
     *                                             "Bob@example.com", "phone": "+5095551234" }`. This data is passed to
     *                                             the `assignment_callback_url` when TaskRouter assigns a Task to the
     *                                             Worker. Defaults to {}.
     * @param friendlyName              A descriptive string that you create to describe the Worker. It can
     *                                             be up to 64 characters long.
     * @param rejectPendingReservations Whether to reject the Worker's pending reservations. This option is
     *                                             only valid if the Worker's new [Activity](https://www.twilio.
     *                                             com/docs/taskrouter/api/activity) resource has its `availability`
     *                                             property set to `False`.
     * @return Response from the API call
     */
    async updateWorker(workspaceSid, sid, ifMatch, activitySid, attributes, friendlyName, rejectPendingReservations, requestOptions) {
        const req = this.createRequest('POST');
        req.baseUrl('default4');
        const mapped = req.prepareArgs({
            workspaceSid: [workspaceSid, string()],
            sid: [sid, string()],
            ifMatch: [ifMatch, optional(string())],
            activitySid: [activitySid, optional(string())],
            attributes: [attributes, optional(string())],
            friendlyName: [friendlyName, optional(string())],
            rejectPendingReservations: [
                rejectPendingReservations,
                optional(boolean()),
            ],
        });
        req.header('Content-Type', 'application/x-www-form-urlencoded');
        req.header('If-Match', mapped.ifMatch);
        req.form({
            ActivitySid: mapped.activitySid,
            Attributes: mapped.attributes,
            FriendlyName: mapped.friendlyName,
            RejectPendingReservations: mapped.rejectPendingReservations,
        });
        req.appendTemplatePath `/v1/Workspaces/${mapped.workspaceSid}/Workers/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(workerSchema, requestOptions);
    }
    /**
     * @param workspaceSid The SID of the Workspace with the Worker to delete.
     * @param sid          The SID of the Worker resource to delete.
     * @param ifMatch      The If-Match HTTP request header
     * @return Response from the API call
     */
    async deleteWorker(workspaceSid, sid, ifMatch, requestOptions) {
        const req = this.createRequest('DELETE');
        req.baseUrl('default4');
        const mapped = req.prepareArgs({
            workspaceSid: [workspaceSid, string()],
            sid: [sid, string()],
            ifMatch: [ifMatch, optional(string())],
        });
        req.header('If-Match', mapped.ifMatch);
        req.appendTemplatePath `/v1/Workspaces/${mapped.workspaceSid}/Workers/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.call(requestOptions);
    }
}
//# sourceMappingURL=taskrouterV1WorkerApi.js.map