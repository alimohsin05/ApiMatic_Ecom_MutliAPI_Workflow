/**
 * Twilio APIsLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */
import { listServiceResponseSchema, } from '../models/listServiceResponse.js';
import { serviceSchema } from '../models/service.js';
import { bigint, number, optional, string } from '../schema.js';
import { BaseApi } from './baseApi.js';
export class ConversationsV1ServiceApi extends BaseApi {
    /**
     * Create a new conversation service on your account
     *
     * @param friendlyName The human-readable name of this service, limited to 256 characters. Optional.
     * @return Response from the API call
     */
    async createService(friendlyName, requestOptions) {
        const req = this.createRequest('POST', '/v1/Services');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({ friendlyName: [friendlyName, string()] });
        req.header('Content-Type', 'application/x-www-form-urlencoded');
        req.form({ FriendlyName: mapped.friendlyName });
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(serviceSchema, requestOptions);
    }
    /**
     * Retrieve a list of all conversation services on your account
     *
     * @param pageSize  How many resources to return in each list page. The default is 50, and the maximum is
     *                            100.
     * @param page      The page index. This value is simply for client state.
     * @param pageToken The page token. This is provided by the API.
     * @return Response from the API call
     */
    async listService(pageSize, page, pageToken, requestOptions) {
        const req = this.createRequest('GET', '/v1/Services');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            pageSize: [pageSize, optional(bigint())],
            page: [page, optional(number())],
            pageToken: [pageToken, optional(string())],
        });
        req.query('PageSize', mapped.pageSize);
        req.query('Page', mapped.page);
        req.query('PageToken', mapped.pageToken);
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(listServiceResponseSchema, requestOptions);
    }
    /**
     * Remove a conversation service with all its nested resources from your account
     *
     * @param sid A 34 character string that uniquely identifies this resource.
     * @return Response from the API call
     */
    async deleteService(sid, requestOptions) {
        const req = this.createRequest('DELETE');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({ sid: [sid, string()] });
        req.appendTemplatePath `/v1/Services/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.call(requestOptions);
    }
    /**
     * Fetch a conversation service from your account
     *
     * @param sid A 34 character string that uniquely identifies this resource.
     * @return Response from the API call
     */
    async fetchService(sid, requestOptions) {
        const req = this.createRequest('GET');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({ sid: [sid, string()] });
        req.appendTemplatePath `/v1/Services/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(serviceSchema, requestOptions);
    }
}
//# sourceMappingURL=conversationsV1ServiceApi.js.map