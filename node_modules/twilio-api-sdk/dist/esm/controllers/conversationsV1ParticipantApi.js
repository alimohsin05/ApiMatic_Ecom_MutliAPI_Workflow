/**
 * Twilio APIsLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */
import { channelWebhookEnabledTypeSchema, } from '../models/channelWebhookEnabledType.js';
import { conversationParticipantSchema, } from '../models/conversationParticipant.js';
import { listConversationParticipantResponseSchema, } from '../models/listConversationParticipantResponse.js';
import { listServiceConversationParticipantResponseSchema, } from '../models/listServiceConversationParticipantResponse.js';
import { serviceConversationParticipantSchema, } from '../models/serviceConversationParticipant.js';
import { bigint, nullable, number, optional, string } from '../schema.js';
import { BaseApi } from './baseApi.js';
export class ConversationsV1ParticipantApi extends BaseApi {
    /**
     * Add a new participant to the conversation
     *
     * @param conversationSid                   The unique ID of the [Conversation](https:
     *                                                                       //www.twilio.
     *                                                                       com/docs/conversations/api/conversation-
     *                                                                       resource) for this participant.
     * @param xTwilioWebhookEnabled             The X-Twilio-Webhook-Enabled HTTP request
     *                                                                       header
     * @param identity                          A unique string identifier for the
     *                                                                       conversation participant as [Conversation
     *                                                                       User](https://www.twilio.
     *                                                                       com/docs/conversations/api/user-resource).
     *                                                                       This parameter is non-null if (and only if)
     *                                                                       the participant is using the Conversations
     *                                                                       SDK to communicate. Limited to 256
     *                                                                       characters.
     * @param messagingBindingAddress           The address of the participant's device, e.
     *                                                                       g. a phone or WhatsApp number. Together with
     *                                                                       the Proxy address, this determines a
     *                                                                       participant uniquely. This field (with
     *                                                                       proxy_address) is only null when the
     *                                                                       participant is interacting from an SDK
     *                                                                       endpoint (see the 'identity' field).
     * @param messagingBindingProxyAddress      The address of the Twilio phone number (or
     *                                                                       WhatsApp number) that the participant is in
     *                                                                       contact with. This field, together with
     *                                                                       participant address, is only null when the
     *                                                                       participant is interacting from an SDK
     *                                                                       endpoint (see the 'identity' field).
     * @param dateCreated                       The date that this resource was created.
     * @param dateUpdated                       The date that this resource was last
     *                                                                       updated.
     * @param attributes                        An optional string metadata field you can
     *                                                                       use to store any data you wish. The string
     *                                                                       value must contain structurally valid JSON
     *                                                                       if specified.  **Note** that if the
     *                                                                       attributes are not set "{}" will be returned.
     * @param messagingBindingProjectedAddress  The address of the Twilio phone number that
     *                                                                       is used in Group MMS. Communication mask for
     *                                                                       the Conversation participant with Identity.
     * @param roleSid                           The SID of a conversation-level
     *                                                                       [Role](https://www.twilio.
     *                                                                       com/docs/conversations/api/role-resource) to
     *                                                                       assign to the participant.
     * @return Response from the API call
     */
    async createConversationParticipant(conversationSid, xTwilioWebhookEnabled, identity, messagingBindingAddress, messagingBindingProxyAddress, dateCreated, dateUpdated, attributes, messagingBindingProjectedAddress, roleSid, requestOptions) {
        const req = this.createRequest('POST');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            conversationSid: [conversationSid, string()],
            xTwilioWebhookEnabled: [
                xTwilioWebhookEnabled,
                optional(channelWebhookEnabledTypeSchema),
            ],
            identity: [identity, optional(string())],
            messagingBindingAddress: [messagingBindingAddress, optional(string())],
            messagingBindingProxyAddress: [
                messagingBindingProxyAddress,
                optional(string()),
            ],
            dateCreated: [dateCreated, optional(string())],
            dateUpdated: [dateUpdated, optional(string())],
            attributes: [attributes, optional(string())],
            messagingBindingProjectedAddress: [
                messagingBindingProjectedAddress,
                optional(string()),
            ],
            roleSid: [roleSid, optional(string())],
        });
        req.header('Content-Type', 'application/x-www-form-urlencoded');
        req.header('X-Twilio-Webhook-Enabled', mapped.xTwilioWebhookEnabled);
        req.form({
            Identity: mapped.identity,
            'MessagingBinding.Address': mapped.messagingBindingAddress,
            'MessagingBinding.ProxyAddress': mapped.messagingBindingProxyAddress,
            DateCreated: mapped.dateCreated,
            DateUpdated: mapped.dateUpdated,
            Attributes: mapped.attributes,
            'MessagingBinding.ProjectedAddress': mapped.messagingBindingProjectedAddress,
            RoleSid: mapped.roleSid,
        });
        req.appendTemplatePath `/v1/Conversations/${mapped.conversationSid}/Participants`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(conversationParticipantSchema, requestOptions);
    }
    /**
     * Retrieve a list of all participants of the conversation
     *
     * @param conversationSid The unique ID of the [Conversation](https://www.twilio.
     *                                  com/docs/conversations/api/conversation-resource) for participants.
     * @param pageSize        How many resources to return in each list page. The default is 50, and the
     *                                  maximum is 100.
     * @param page            The page index. This value is simply for client state.
     * @param pageToken       The page token. This is provided by the API.
     * @return Response from the API call
     */
    async listConversationParticipant(conversationSid, pageSize, page, pageToken, requestOptions) {
        const req = this.createRequest('GET');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            conversationSid: [conversationSid, string()],
            pageSize: [pageSize, optional(bigint())],
            page: [page, optional(number())],
            pageToken: [pageToken, optional(string())],
        });
        req.query('PageSize', mapped.pageSize);
        req.query('Page', mapped.page);
        req.query('PageToken', mapped.pageToken);
        req.appendTemplatePath `/v1/Conversations/${mapped.conversationSid}/Participants`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(listConversationParticipantResponseSchema, requestOptions);
    }
    /**
     * Update an existing participant in the conversation
     *
     * @param conversationSid                   The unique ID of the [Conversation](https:
     *                                                                       //www.twilio.
     *                                                                       com/docs/conversations/api/conversation-
     *                                                                       resource) for this participant.
     * @param sid                               A 34 character string that uniquely
     *                                                                       identifies this resource.
     * @param xTwilioWebhookEnabled             The X-Twilio-Webhook-Enabled HTTP request
     *                                                                       header
     * @param dateCreated                       The date that this resource was created.
     * @param dateUpdated                       The date that this resource was last
     *                                                                       updated.
     * @param attributes                        An optional string metadata field you can
     *                                                                       use to store any data you wish. The string
     *                                                                       value must contain structurally valid JSON
     *                                                                       if specified.  **Note** that if the
     *                                                                       attributes are not set "{}" will be returned.
     * @param roleSid                           The SID of a conversation-level
     *                                                                       [Role](https://www.twilio.
     *                                                                       com/docs/conversations/api/role-resource) to
     *                                                                       assign to the participant.
     * @param messagingBindingProxyAddress      The address of the Twilio phone number that
     *                                                                       the participant is in contact with. 'null'
     *                                                                       value will remove it.
     * @param messagingBindingProjectedAddress  The address of the Twilio phone number that
     *                                                                       is used in Group MMS. 'null' value will
     *                                                                       remove it.
     * @param identity                          A unique string identifier for the
     *                                                                       conversation participant as [Conversation
     *                                                                       User](https://www.twilio.
     *                                                                       com/docs/conversations/api/user-resource).
     *                                                                       This parameter is non-null if (and only if)
     *                                                                       the participant is using the Conversations
     *                                                                       SDK to communicate. Limited to 256
     *                                                                       characters.
     * @param lastReadMessageIndex              Index of last “read” message in the
     *                                                                       [Conversation](https://www.twilio.
     *                                                                       com/docs/conversations/api/conversation-
     *                                                                       resource) for the Participant.
     * @param lastReadTimestamp                 Timestamp of last “read” message in the
     *                                                                       [Conversation](https://www.twilio.
     *                                                                       com/docs/conversations/api/conversation-
     *                                                                       resource) for the Participant.
     * @return Response from the API call
     */
    async updateConversationParticipant(conversationSid, sid, xTwilioWebhookEnabled, dateCreated, dateUpdated, attributes, roleSid, messagingBindingProxyAddress, messagingBindingProjectedAddress, identity, lastReadMessageIndex, lastReadTimestamp, requestOptions) {
        const req = this.createRequest('POST');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            conversationSid: [conversationSid, string()],
            sid: [sid, string()],
            xTwilioWebhookEnabled: [
                xTwilioWebhookEnabled,
                optional(channelWebhookEnabledTypeSchema),
            ],
            dateCreated: [dateCreated, optional(string())],
            dateUpdated: [dateUpdated, optional(string())],
            attributes: [attributes, optional(string())],
            roleSid: [roleSid, optional(string())],
            messagingBindingProxyAddress: [
                messagingBindingProxyAddress,
                optional(string()),
            ],
            messagingBindingProjectedAddress: [
                messagingBindingProjectedAddress,
                optional(string()),
            ],
            identity: [identity, optional(string())],
            lastReadMessageIndex: [
                lastReadMessageIndex,
                optional(nullable(number())),
            ],
            lastReadTimestamp: [lastReadTimestamp, optional(string())],
        });
        req.header('Content-Type', 'application/x-www-form-urlencoded');
        req.header('X-Twilio-Webhook-Enabled', mapped.xTwilioWebhookEnabled);
        req.form({
            DateCreated: mapped.dateCreated,
            DateUpdated: mapped.dateUpdated,
            Attributes: mapped.attributes,
            RoleSid: mapped.roleSid,
            'MessagingBinding.ProxyAddress': mapped.messagingBindingProxyAddress,
            'MessagingBinding.ProjectedAddress': mapped.messagingBindingProjectedAddress,
            Identity: mapped.identity,
            LastReadMessageIndex: mapped.lastReadMessageIndex,
            LastReadTimestamp: mapped.lastReadTimestamp,
        });
        req.appendTemplatePath `/v1/Conversations/${mapped.conversationSid}/Participants/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(conversationParticipantSchema, requestOptions);
    }
    /**
     * Remove a participant from the conversation
     *
     * @param conversationSid          The unique ID of the [Conversation](https://www.
     *                                                              twilio.com/docs/conversations/api/conversation-
     *                                                              resource) for this participant.
     * @param sid                      A 34 character string that uniquely identifies this
     *                                                              resource.
     * @param xTwilioWebhookEnabled    The X-Twilio-Webhook-Enabled HTTP request header
     * @return Response from the API call
     */
    async deleteConversationParticipant(conversationSid, sid, xTwilioWebhookEnabled, requestOptions) {
        const req = this.createRequest('DELETE');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            conversationSid: [conversationSid, string()],
            sid: [sid, string()],
            xTwilioWebhookEnabled: [
                xTwilioWebhookEnabled,
                optional(channelWebhookEnabledTypeSchema),
            ],
        });
        req.header('X-Twilio-Webhook-Enabled', mapped.xTwilioWebhookEnabled);
        req.appendTemplatePath `/v1/Conversations/${mapped.conversationSid}/Participants/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.call(requestOptions);
    }
    /**
     * Fetch a participant of the conversation
     *
     * @param conversationSid The unique ID of the [Conversation](https://www.twilio.
     *                                  com/docs/conversations/api/conversation-resource) for this participant.
     * @param sid             A 34 character string that uniquely identifies this resource. Alternatively, you
     *                                  can pass a Participant's `identity` rather than the SID.
     * @return Response from the API call
     */
    async fetchConversationParticipant(conversationSid, sid, requestOptions) {
        const req = this.createRequest('GET');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            conversationSid: [conversationSid, string()],
            sid: [sid, string()],
        });
        req.appendTemplatePath `/v1/Conversations/${mapped.conversationSid}/Participants/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(conversationParticipantSchema, requestOptions);
    }
    /**
     * Add a new participant to the conversation in a specific service
     *
     * @param chatServiceSid                    The SID of the [Conversation Service](https:
     *                                                                       //www.twilio.
     *                                                                       com/docs/conversations/api/service-resource)
     *                                                                       the Participant resource is associated with.
     * @param conversationSid                   The unique ID of the [Conversation](https:
     *                                                                       //www.twilio.
     *                                                                       com/docs/conversations/api/conversation-
     *                                                                       resource) for this participant.
     * @param xTwilioWebhookEnabled             The X-Twilio-Webhook-Enabled HTTP request
     *                                                                       header
     * @param identity                          A unique string identifier for the
     *                                                                       conversation participant as [Conversation
     *                                                                       User](https://www.twilio.
     *                                                                       com/docs/conversations/api/user-resource).
     *                                                                       This parameter is non-null if (and only if)
     *                                                                       the participant is using the [Conversation
     *                                                                       SDK](https://www.twilio.
     *                                                                       com/docs/conversations/sdk-overview) to
     *                                                                       communicate. Limited to 256 characters.
     * @param messagingBindingAddress           The address of the participant's device, e.
     *                                                                       g. a phone or WhatsApp number. Together with
     *                                                                       the Proxy address, this determines a
     *                                                                       participant uniquely. This field (with
     *                                                                       `proxy_address`) is only null when the
     *                                                                       participant is interacting from an SDK
     *                                                                       endpoint (see the `identity` field).
     * @param messagingBindingProxyAddress      The address of the Twilio phone number (or
     *                                                                       WhatsApp number) that the participant is in
     *                                                                       contact with. This field, together with
     *                                                                       participant address, is only null when the
     *                                                                       participant is interacting from an SDK
     *                                                                       endpoint (see the `identity` field).
     * @param dateCreated                       The date on which this resource was created.
     * @param dateUpdated                       The date on which this resource was last
     *                                                                       updated.
     * @param attributes                        An optional string metadata field you can
     *                                                                       use to store any data you wish. The string
     *                                                                       value must contain structurally valid JSON
     *                                                                       if specified.  **Note** that if the
     *                                                                       attributes are not set `{}` will be returned.
     * @param messagingBindingProjectedAddress  The address of the Twilio phone number that
     *                                                                       is used in Group MMS.
     * @param roleSid                           The SID of a conversation-level
     *                                                                       [Role](https://www.twilio.
     *                                                                       com/docs/conversations/api/role-resource) to
     *                                                                       assign to the participant.
     * @return Response from the API call
     */
    async createServiceConversationParticipant(chatServiceSid, conversationSid, xTwilioWebhookEnabled, identity, messagingBindingAddress, messagingBindingProxyAddress, dateCreated, dateUpdated, attributes, messagingBindingProjectedAddress, roleSid, requestOptions) {
        const req = this.createRequest('POST');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            chatServiceSid: [chatServiceSid, string()],
            conversationSid: [conversationSid, string()],
            xTwilioWebhookEnabled: [
                xTwilioWebhookEnabled,
                optional(channelWebhookEnabledTypeSchema),
            ],
            identity: [identity, optional(string())],
            messagingBindingAddress: [messagingBindingAddress, optional(string())],
            messagingBindingProxyAddress: [
                messagingBindingProxyAddress,
                optional(string()),
            ],
            dateCreated: [dateCreated, optional(string())],
            dateUpdated: [dateUpdated, optional(string())],
            attributes: [attributes, optional(string())],
            messagingBindingProjectedAddress: [
                messagingBindingProjectedAddress,
                optional(string()),
            ],
            roleSid: [roleSid, optional(string())],
        });
        req.header('Content-Type', 'application/x-www-form-urlencoded');
        req.header('X-Twilio-Webhook-Enabled', mapped.xTwilioWebhookEnabled);
        req.form({
            Identity: mapped.identity,
            'MessagingBinding.Address': mapped.messagingBindingAddress,
            'MessagingBinding.ProxyAddress': mapped.messagingBindingProxyAddress,
            DateCreated: mapped.dateCreated,
            DateUpdated: mapped.dateUpdated,
            Attributes: mapped.attributes,
            'MessagingBinding.ProjectedAddress': mapped.messagingBindingProjectedAddress,
            RoleSid: mapped.roleSid,
        });
        req.appendTemplatePath `/v1/Services/${mapped.chatServiceSid}/Conversations/${mapped.conversationSid}/Participants`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(serviceConversationParticipantSchema, requestOptions);
    }
    /**
     * Retrieve a list of all participants of the conversation
     *
     * @param chatServiceSid  The SID of the [Conversation Service](https://www.twilio.
     *                                  com/docs/conversations/api/service-resource) the Participant resource is
     *                                  associated with.
     * @param conversationSid The unique ID of the [Conversation](https://www.twilio.
     *                                  com/docs/conversations/api/conversation-resource) for participants.
     * @param pageSize        How many resources to return in each list page. The default is 50, and the
     *                                  maximum is 100.
     * @param page            The page index. This value is simply for client state.
     * @param pageToken       The page token. This is provided by the API.
     * @return Response from the API call
     */
    async listServiceConversationParticipant(chatServiceSid, conversationSid, pageSize, page, pageToken, requestOptions) {
        const req = this.createRequest('GET');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            chatServiceSid: [chatServiceSid, string()],
            conversationSid: [conversationSid, string()],
            pageSize: [pageSize, optional(bigint())],
            page: [page, optional(number())],
            pageToken: [pageToken, optional(string())],
        });
        req.query('PageSize', mapped.pageSize);
        req.query('Page', mapped.page);
        req.query('PageToken', mapped.pageToken);
        req.appendTemplatePath `/v1/Services/${mapped.chatServiceSid}/Conversations/${mapped.conversationSid}/Participants`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(listServiceConversationParticipantResponseSchema, requestOptions);
    }
    /**
     * Update an existing participant in the conversation
     *
     * @param chatServiceSid                    The SID of the [Conversation Service](https:
     *                                                                       //www.twilio.
     *                                                                       com/docs/conversations/api/service-resource)
     *                                                                       the Participant resource is associated with.
     * @param conversationSid                   The unique ID of the [Conversation](https:
     *                                                                       //www.twilio.
     *                                                                       com/docs/conversations/api/conversation-
     *                                                                       resource) for this participant.
     * @param sid                               A 34 character string that uniquely
     *                                                                       identifies this resource.
     * @param xTwilioWebhookEnabled             The X-Twilio-Webhook-Enabled HTTP request
     *                                                                       header
     * @param dateCreated                       The date on which this resource was created.
     * @param dateUpdated                       The date on which this resource was last
     *                                                                       updated.
     * @param identity                          A unique string identifier for the
     *                                                                       conversation participant as [Conversation
     *                                                                       User](https://www.twilio.
     *                                                                       com/docs/conversations/api/user-resource).
     *                                                                       This parameter is non-null if (and only if)
     *                                                                       the participant is using the [Conversation
     *                                                                       SDK](https://www.twilio.
     *                                                                       com/docs/conversations/sdk-overview) to
     *                                                                       communicate. Limited to 256 characters.
     * @param attributes                        An optional string metadata field you can
     *                                                                       use to store any data you wish. The string
     *                                                                       value must contain structurally valid JSON
     *                                                                       if specified.  **Note** that if the
     *                                                                       attributes are not set `{}` will be returned.
     * @param roleSid                           The SID of a conversation-level
     *                                                                       [Role](https://www.twilio.
     *                                                                       com/docs/conversations/api/role-resource) to
     *                                                                       assign to the participant.
     * @param messagingBindingProxyAddress      The address of the Twilio phone number that
     *                                                                       the participant is in contact with. 'null'
     *                                                                       value will remove it.
     * @param messagingBindingProjectedAddress  The address of the Twilio phone number that
     *                                                                       is used in Group MMS. 'null' value will
     *                                                                       remove it.
     * @param lastReadMessageIndex              Index of last “read” message in the
     *                                                                       [Conversation](https://www.twilio.
     *                                                                       com/docs/conversations/api/conversation-
     *                                                                       resource) for the Participant.
     * @param lastReadTimestamp                 Timestamp of last “read” message in the
     *                                                                       [Conversation](https://www.twilio.
     *                                                                       com/docs/conversations/api/conversation-
     *                                                                       resource) for the Participant.
     * @return Response from the API call
     */
    async updateServiceConversationParticipant(chatServiceSid, conversationSid, sid, xTwilioWebhookEnabled, dateCreated, dateUpdated, identity, attributes, roleSid, messagingBindingProxyAddress, messagingBindingProjectedAddress, lastReadMessageIndex, lastReadTimestamp, requestOptions) {
        const req = this.createRequest('POST');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            chatServiceSid: [chatServiceSid, string()],
            conversationSid: [conversationSid, string()],
            sid: [sid, string()],
            xTwilioWebhookEnabled: [
                xTwilioWebhookEnabled,
                optional(channelWebhookEnabledTypeSchema),
            ],
            dateCreated: [dateCreated, optional(string())],
            dateUpdated: [dateUpdated, optional(string())],
            identity: [identity, optional(string())],
            attributes: [attributes, optional(string())],
            roleSid: [roleSid, optional(string())],
            messagingBindingProxyAddress: [
                messagingBindingProxyAddress,
                optional(string()),
            ],
            messagingBindingProjectedAddress: [
                messagingBindingProjectedAddress,
                optional(string()),
            ],
            lastReadMessageIndex: [
                lastReadMessageIndex,
                optional(nullable(number())),
            ],
            lastReadTimestamp: [lastReadTimestamp, optional(string())],
        });
        req.header('Content-Type', 'application/x-www-form-urlencoded');
        req.header('X-Twilio-Webhook-Enabled', mapped.xTwilioWebhookEnabled);
        req.form({
            DateCreated: mapped.dateCreated,
            DateUpdated: mapped.dateUpdated,
            Identity: mapped.identity,
            Attributes: mapped.attributes,
            RoleSid: mapped.roleSid,
            'MessagingBinding.ProxyAddress': mapped.messagingBindingProxyAddress,
            'MessagingBinding.ProjectedAddress': mapped.messagingBindingProjectedAddress,
            LastReadMessageIndex: mapped.lastReadMessageIndex,
            LastReadTimestamp: mapped.lastReadTimestamp,
        });
        req.appendTemplatePath `/v1/Services/${mapped.chatServiceSid}/Conversations/${mapped.conversationSid}/Participants/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(serviceConversationParticipantSchema, requestOptions);
    }
    /**
     * Remove a participant from the conversation
     *
     * @param chatServiceSid           The SID of the [Conversation Service](https://www.
     *                                                              twilio.com/docs/conversations/api/service-resource)
     *                                                              the Participant resource is associated with.
     * @param conversationSid          The unique ID of the [Conversation](https://www.
     *                                                              twilio.com/docs/conversations/api/conversation-
     *                                                              resource) for this participant.
     * @param sid                      A 34 character string that uniquely identifies this
     *                                                              resource.
     * @param xTwilioWebhookEnabled    The X-Twilio-Webhook-Enabled HTTP request header
     * @return Response from the API call
     */
    async deleteServiceConversationParticipant(chatServiceSid, conversationSid, sid, xTwilioWebhookEnabled, requestOptions) {
        const req = this.createRequest('DELETE');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            chatServiceSid: [chatServiceSid, string()],
            conversationSid: [conversationSid, string()],
            sid: [sid, string()],
            xTwilioWebhookEnabled: [
                xTwilioWebhookEnabled,
                optional(channelWebhookEnabledTypeSchema),
            ],
        });
        req.header('X-Twilio-Webhook-Enabled', mapped.xTwilioWebhookEnabled);
        req.appendTemplatePath `/v1/Services/${mapped.chatServiceSid}/Conversations/${mapped.conversationSid}/Participants/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.call(requestOptions);
    }
    /**
     * Fetch a participant of the conversation
     *
     * @param chatServiceSid  The SID of the [Conversation Service](https://www.twilio.
     *                                  com/docs/conversations/api/service-resource) the Participant resource is
     *                                  associated with.
     * @param conversationSid The unique ID of the [Conversation](https://www.twilio.
     *                                  com/docs/conversations/api/conversation-resource) for this participant.
     * @param sid             A 34 character string that uniquely identifies this resource. Alternatively, you
     *                                  can pass a Participant's `identity` rather than the SID.
     * @return Response from the API call
     */
    async fetchServiceConversationParticipant(chatServiceSid, conversationSid, sid, requestOptions) {
        const req = this.createRequest('GET');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            chatServiceSid: [chatServiceSid, string()],
            conversationSid: [conversationSid, string()],
            sid: [sid, string()],
        });
        req.appendTemplatePath `/v1/Services/${mapped.chatServiceSid}/Conversations/${mapped.conversationSid}/Participants/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(serviceConversationParticipantSchema, requestOptions);
    }
}
//# sourceMappingURL=conversationsV1ParticipantApi.js.map