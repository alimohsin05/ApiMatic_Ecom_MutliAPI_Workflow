/**
 * Twilio APIsLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */
import { credentialPublicKeySchema, } from '../models/credentialPublicKey.js';
import { listCredentialPublicKeyResponseSchema, } from '../models/listCredentialPublicKeyResponse.js';
import { bigint, number, optional, string } from '../schema.js';
import { BaseApi } from './baseApi.js';
export class AccountsV1PublicKeyApi extends BaseApi {
    /**
     * Retrieves a collection of Public Key Credentials belonging to the account used to make the request
     *
     * @param pageSize  How many resources to return in each list page. The default is 50, and the maximum is
     *                            1000.
     * @param page      The page index. This value is simply for client state.
     * @param pageToken The page token. This is provided by the API.
     * @return Response from the API call
     */
    async listCredentialPublicKey(pageSize, page, pageToken, requestOptions) {
        const req = this.createRequest('GET', '/v1/Credentials/PublicKeys');
        const mapped = req.prepareArgs({
            pageSize: [pageSize, optional(bigint())],
            page: [page, optional(number())],
            pageToken: [pageToken, optional(string())],
        });
        req.query('PageSize', mapped.pageSize);
        req.query('Page', mapped.page);
        req.query('PageToken', mapped.pageToken);
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(listCredentialPublicKeyResponseSchema, requestOptions);
    }
    /**
     * Create a new Public Key Credential
     *
     * @param publicKey    A URL encoded representation of the public key. For example, `-----BEGIN PUBLIC KEY-
     *                               ----MIIBIjANB.pa9xQIDAQAB-----END PUBLIC KEY-----`
     * @param friendlyName A descriptive string that you create to describe the resource. It can be up to 64
     *                               characters long.
     * @param accountSid   The SID of the Subaccount that this Credential should be associated with. Must be a
     *                               valid Subaccount of the account issuing the request
     * @return Response from the API call
     */
    async createCredentialPublicKey(publicKey, friendlyName, accountSid, requestOptions) {
        const req = this.createRequest('POST', '/v1/Credentials/PublicKeys');
        const mapped = req.prepareArgs({
            publicKey: [publicKey, string()],
            friendlyName: [friendlyName, optional(string())],
            accountSid: [accountSid, optional(string())],
        });
        req.header('Content-Type', 'application/x-www-form-urlencoded');
        req.form({
            PublicKey: mapped.publicKey,
            FriendlyName: mapped.friendlyName,
            AccountSid: mapped.accountSid,
        });
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(credentialPublicKeySchema, requestOptions);
    }
    /**
     * Fetch the public key specified by the provided Credential Sid
     *
     * @param sid The Twilio-provided string that uniquely identifies the PublicKey resource to fetch.
     * @return Response from the API call
     */
    async fetchCredentialPublicKey(sid, requestOptions) {
        const req = this.createRequest('GET');
        const mapped = req.prepareArgs({ sid: [sid, string()] });
        req.appendTemplatePath `/v1/Credentials/PublicKeys/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(credentialPublicKeySchema, requestOptions);
    }
    /**
     * Modify the properties of a given Account
     *
     * @param sid          The Twilio-provided string that uniquely identifies the PublicKey resource to
     *                               update.
     * @param friendlyName A descriptive string that you create to describe the resource. It can be up to 64
     *                               characters long.
     * @return Response from the API call
     */
    async updateCredentialPublicKey(sid, friendlyName, requestOptions) {
        const req = this.createRequest('POST');
        const mapped = req.prepareArgs({
            sid: [sid, string()],
            friendlyName: [friendlyName, optional(string())],
        });
        req.header('Content-Type', 'application/x-www-form-urlencoded');
        req.form({ FriendlyName: mapped.friendlyName });
        req.appendTemplatePath `/v1/Credentials/PublicKeys/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(credentialPublicKeySchema, requestOptions);
    }
    /**
     * Delete a Credential from your account
     *
     * @param sid The Twilio-provided string that uniquely identifies the PublicKey resource to delete.
     * @return Response from the API call
     */
    async deleteCredentialPublicKey(sid, requestOptions) {
        const req = this.createRequest('DELETE');
        const mapped = req.prepareArgs({ sid: [sid, string()] });
        req.appendTemplatePath `/v1/Credentials/PublicKeys/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.call(requestOptions);
    }
}
//# sourceMappingURL=accountsV1PublicKeyApi.js.map