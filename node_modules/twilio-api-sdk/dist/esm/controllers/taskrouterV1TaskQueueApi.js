/**
 * Twilio APIsLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */
import { listTaskQueueResponseSchema, } from '../models/listTaskQueueResponse.js';
import { taskQueueSchema } from '../models/taskQueue.js';
import { taskQueueTaskOrderSchema, } from '../models/taskQueueTaskOrder.js';
import { bigint, number, optional, string } from '../schema.js';
import { BaseApi } from './baseApi.js';
export class TaskrouterV1TaskQueueApi extends BaseApi {
    /**
     * @param workspaceSid The SID of the Workspace with the TaskQueue to fetch.
     * @param sid          The SID of the TaskQueue resource to fetch.
     * @return Response from the API call
     */
    async fetchTaskQueue(workspaceSid, sid, requestOptions) {
        const req = this.createRequest('GET');
        req.baseUrl('default4');
        const mapped = req.prepareArgs({
            workspaceSid: [workspaceSid, string()],
            sid: [sid, string()],
        });
        req.appendTemplatePath `/v1/Workspaces/${mapped.workspaceSid}/TaskQueues/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(taskQueueSchema, requestOptions);
    }
    /**
     * @param workspaceSid           The SID of the Workspace with the TaskQueue to update.
     * @param sid                    The SID of the TaskQueue resource to update.
     * @param friendlyName           A descriptive string that you create to describe the
     *                                                     TaskQueue. For example `Support-Tier 1`, `Sales`, or
     *                                                     `Escalation`.
     * @param targetWorkers          A string describing the Worker selection criteria for any
     *                                                     Tasks that enter the TaskQueue. For example '"language" ==
     *                                                     "spanish"' If no TargetWorkers parameter is provided, Tasks
     *                                                     will wait in the queue until they are either deleted or moved
     *                                                     to another queue. Additional examples on how to describing
     *                                                     Worker selection criteria below.
     * @param reservationActivitySid The SID of the Activity to assign Workers when a task is
     *                                                     reserved for them.
     * @param assignmentActivitySid  The SID of the Activity to assign Workers when a task is
     *                                                     assigned for them.
     * @param maxReservedWorkers     The maximum number of Workers to create reservations for the
     *                                                     assignment of a task while in the queue. Maximum of 50.
     * @param taskOrder              How Tasks will be assigned to Workers. Set this parameter to
     *                                                     `LIFO` to assign most recently created Task first or `FIFO` to
     *                                                     assign the oldest Task. Default is FIFO. [Click here](https:
     *                                                     //www.twilio.com/docs/taskrouter/queue-ordering-last-first-out-
     *                                                     lifo) to learn more.
     * @return Response from the API call
     */
    async updateTaskQueue(workspaceSid, sid, friendlyName, targetWorkers, reservationActivitySid, assignmentActivitySid, maxReservedWorkers, taskOrder, requestOptions) {
        const req = this.createRequest('POST');
        req.baseUrl('default4');
        const mapped = req.prepareArgs({
            workspaceSid: [workspaceSid, string()],
            sid: [sid, string()],
            friendlyName: [friendlyName, optional(string())],
            targetWorkers: [targetWorkers, optional(string())],
            reservationActivitySid: [reservationActivitySid, optional(string())],
            assignmentActivitySid: [assignmentActivitySid, optional(string())],
            maxReservedWorkers: [maxReservedWorkers, optional(number())],
            taskOrder: [taskOrder, optional(taskQueueTaskOrderSchema)],
        });
        req.header('Content-Type', 'application/x-www-form-urlencoded');
        req.form({
            FriendlyName: mapped.friendlyName,
            TargetWorkers: mapped.targetWorkers,
            ReservationActivitySid: mapped.reservationActivitySid,
            AssignmentActivitySid: mapped.assignmentActivitySid,
            MaxReservedWorkers: mapped.maxReservedWorkers,
            TaskOrder: mapped.taskOrder,
        });
        req.appendTemplatePath `/v1/Workspaces/${mapped.workspaceSid}/TaskQueues/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(taskQueueSchema, requestOptions);
    }
    /**
     * @param workspaceSid The SID of the Workspace with the TaskQueue to delete.
     * @param sid          The SID of the TaskQueue resource to delete.
     * @return Response from the API call
     */
    async deleteTaskQueue(workspaceSid, sid, requestOptions) {
        const req = this.createRequest('DELETE');
        req.baseUrl('default4');
        const mapped = req.prepareArgs({
            workspaceSid: [workspaceSid, string()],
            sid: [sid, string()],
        });
        req.appendTemplatePath `/v1/Workspaces/${mapped.workspaceSid}/TaskQueues/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.call(requestOptions);
    }
    /**
     * @param workspaceSid             The SID of the Workspace with the TaskQueue to read.
     * @param friendlyName             The `friendly_name` of the TaskQueue resources to read.
     * @param evaluateWorkerAttributes The attributes of the Workers to read. Returns the TaskQueues with
     *                                           Workers that match the attributes specified in this parameter.
     * @param workerSid                The SID of the Worker with the TaskQueue resources to read.
     * @param ordering                 Sorting parameter for TaskQueues
     * @param pageSize                 How many resources to return in each list page. The default is 50, and
     *                                           the maximum is 1000.
     * @param page                     The page index. This value is simply for client state.
     * @param pageToken                The page token. This is provided by the API.
     * @return Response from the API call
     */
    async listTaskQueue(workspaceSid, friendlyName, evaluateWorkerAttributes, workerSid, ordering, pageSize, page, pageToken, requestOptions) {
        const req = this.createRequest('GET');
        req.baseUrl('default4');
        const mapped = req.prepareArgs({
            workspaceSid: [workspaceSid, string()],
            friendlyName: [friendlyName, optional(string())],
            evaluateWorkerAttributes: [evaluateWorkerAttributes, optional(string())],
            workerSid: [workerSid, optional(string())],
            ordering: [ordering, optional(string())],
            pageSize: [pageSize, optional(bigint())],
            page: [page, optional(number())],
            pageToken: [pageToken, optional(string())],
        });
        req.query('FriendlyName', mapped.friendlyName);
        req.query('EvaluateWorkerAttributes', mapped.evaluateWorkerAttributes);
        req.query('WorkerSid', mapped.workerSid);
        req.query('Ordering', mapped.ordering);
        req.query('PageSize', mapped.pageSize);
        req.query('Page', mapped.page);
        req.query('PageToken', mapped.pageToken);
        req.appendTemplatePath `/v1/Workspaces/${mapped.workspaceSid}/TaskQueues`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(listTaskQueueResponseSchema, requestOptions);
    }
    /**
     * @param workspaceSid           The SID of the Workspace that the new TaskQueue belongs to.
     * @param friendlyName           A descriptive string that you create to describe the
     *                                                     TaskQueue. For example `Support-Tier 1`, `Sales`, or
     *                                                     `Escalation`.
     * @param targetWorkers          A string that describes the Worker selection criteria for any
     *                                                     Tasks that enter the TaskQueue. For example, `'"language" ==
     *                                                     "spanish"'`. The default value is `1==1`. If this value is
     *                                                     empty, Tasks will wait in the TaskQueue until they are deleted
     *                                                     or moved to another TaskQueue. For more information about
     *                                                     Worker selection, see [Describing Worker selection
     *                                                     criteria](https://www.twilio.
     *                                                     com/docs/taskrouter/api/taskqueues#target-workers).
     * @param maxReservedWorkers     The maximum number of Workers to reserve for the assignment
     *                                                     of a Task in the queue. Can be an integer between 1 and 50,
     *                                                     inclusive and defaults to 1.
     * @param taskOrder              How Tasks will be assigned to Workers. Set this parameter to
     *                                                     `LIFO` to assign most recently created Task first or `FIFO` to
     *                                                     assign the oldest Task. Default is FIFO. [Click here](https:
     *                                                     //www.twilio.com/docs/taskrouter/queue-ordering-last-first-out-
     *                                                     lifo) to learn more.
     * @param reservationActivitySid The SID of the Activity to assign Workers when a task is
     *                                                     reserved for them.
     * @param assignmentActivitySid  The SID of the Activity to assign Workers when a task is
     *                                                     assigned to them.
     * @return Response from the API call
     */
    async createTaskQueue(workspaceSid, friendlyName, targetWorkers, maxReservedWorkers, taskOrder, reservationActivitySid, assignmentActivitySid, requestOptions) {
        const req = this.createRequest('POST');
        req.baseUrl('default4');
        const mapped = req.prepareArgs({
            workspaceSid: [workspaceSid, string()],
            friendlyName: [friendlyName, string()],
            targetWorkers: [targetWorkers, optional(string())],
            maxReservedWorkers: [maxReservedWorkers, optional(number())],
            taskOrder: [taskOrder, optional(taskQueueTaskOrderSchema)],
            reservationActivitySid: [reservationActivitySid, optional(string())],
            assignmentActivitySid: [assignmentActivitySid, optional(string())],
        });
        req.header('Content-Type', 'application/x-www-form-urlencoded');
        req.form({
            FriendlyName: mapped.friendlyName,
            TargetWorkers: mapped.targetWorkers,
            MaxReservedWorkers: mapped.maxReservedWorkers,
            TaskOrder: mapped.taskOrder,
            ReservationActivitySid: mapped.reservationActivitySid,
            AssignmentActivitySid: mapped.assignmentActivitySid,
        });
        req.appendTemplatePath `/v1/Workspaces/${mapped.workspaceSid}/TaskQueues`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(taskQueueSchema, requestOptions);
    }
}
//# sourceMappingURL=taskrouterV1TaskQueueApi.js.map