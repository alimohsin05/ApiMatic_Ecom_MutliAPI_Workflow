/**
 * Twilio APIsLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */
import { conversationMessageReceiptSchema, } from '../models/conversationMessageReceipt.js';
import { listConversationMessageReceiptResponseSchema, } from '../models/listConversationMessageReceiptResponse.js';
import { listServiceConversationMessageReceiptResponseSchema, } from '../models/listServiceConversationMessageReceiptResponse.js';
import { serviceConversationMessageReceiptSchema, } from '../models/serviceConversationMessageReceipt.js';
import { bigint, number, optional, string } from '../schema.js';
import { BaseApi } from './baseApi.js';
export class ConversationsV1DeliveryReceiptApi extends BaseApi {
    /**
     * Fetch the delivery and read receipts of the conversation message
     *
     * @param conversationSid The unique ID of the [Conversation](https://www.twilio.
     *                                  com/docs/conversations/api/conversation-resource) for this message.
     * @param messageSid      The SID of the message within a [Conversation](https://www.twilio.
     *                                  com/docs/conversations/api/conversation-resource) the delivery receipt belongs to.
     * @param sid             A 34 character string that uniquely identifies this resource.
     * @return Response from the API call
     */
    async fetchConversationMessageReceipt(conversationSid, messageSid, sid, requestOptions) {
        const req = this.createRequest('GET');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            conversationSid: [conversationSid, string()],
            messageSid: [messageSid, string()],
            sid: [sid, string()],
        });
        req.appendTemplatePath `/v1/Conversations/${mapped.conversationSid}/Messages/${mapped.messageSid}/Receipts/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(conversationMessageReceiptSchema, requestOptions);
    }
    /**
     * Retrieve a list of all delivery and read receipts of the conversation message
     *
     * @param conversationSid The unique ID of the [Conversation](https://www.twilio.
     *                                  com/docs/conversations/api/conversation-resource) for this message.
     * @param messageSid      The SID of the message within a [Conversation](https://www.twilio.
     *                                  com/docs/conversations/api/conversation-resource) the delivery receipt belongs to.
     * @param pageSize        How many resources to return in each list page. The default is 50, and the
     *                                  maximum is 50.
     * @param page            The page index. This value is simply for client state.
     * @param pageToken       The page token. This is provided by the API.
     * @return Response from the API call
     */
    async listConversationMessageReceipt(conversationSid, messageSid, pageSize, page, pageToken, requestOptions) {
        const req = this.createRequest('GET');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            conversationSid: [conversationSid, string()],
            messageSid: [messageSid, string()],
            pageSize: [pageSize, optional(bigint())],
            page: [page, optional(number())],
            pageToken: [pageToken, optional(string())],
        });
        req.query('PageSize', mapped.pageSize);
        req.query('Page', mapped.page);
        req.query('PageToken', mapped.pageToken);
        req.appendTemplatePath `/v1/Conversations/${mapped.conversationSid}/Messages/${mapped.messageSid}/Receipts`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(listConversationMessageReceiptResponseSchema, requestOptions);
    }
    /**
     * Fetch the delivery and read receipts of the conversation message
     *
     * @param chatServiceSid  The SID of the [Conversation Service](https://www.twilio.
     *                                  com/docs/conversations/api/service-resource) the Message resource is associated
     *                                  with.
     * @param conversationSid The unique ID of the [Conversation](https://www.twilio.
     *                                  com/docs/conversations/api/conversation-resource) for this message.
     * @param messageSid      The SID of the message within a [Conversation](https://www.twilio.
     *                                  com/docs/conversations/api/conversation-resource) the delivery receipt belongs to.
     * @param sid             A 34 character string that uniquely identifies this resource.
     * @return Response from the API call
     */
    async fetchServiceConversationMessageReceipt(chatServiceSid, conversationSid, messageSid, sid, requestOptions) {
        const req = this.createRequest('GET');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            chatServiceSid: [chatServiceSid, string()],
            conversationSid: [conversationSid, string()],
            messageSid: [messageSid, string()],
            sid: [sid, string()],
        });
        req.appendTemplatePath `/v1/Services/${mapped.chatServiceSid}/Conversations/${mapped.conversationSid}/Messages/${mapped.messageSid}/Receipts/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(serviceConversationMessageReceiptSchema, requestOptions);
    }
    /**
     * Retrieve a list of all delivery and read receipts of the conversation message
     *
     * @param chatServiceSid  The SID of the [Conversation Service](https://www.twilio.
     *                                  com/docs/conversations/api/service-resource) the Message resource is associated
     *                                  with.
     * @param conversationSid The unique ID of the [Conversation](https://www.twilio.
     *                                  com/docs/conversations/api/conversation-resource) for this message.
     * @param messageSid      The SID of the message within a [Conversation](https://www.twilio.
     *                                  com/docs/conversations/api/conversation-resource) the delivery receipt belongs to.
     * @param pageSize        How many resources to return in each list page. The default is 50, and the
     *                                  maximum is 50.
     * @param page            The page index. This value is simply for client state.
     * @param pageToken       The page token. This is provided by the API.
     * @return Response from the API call
     */
    async listServiceConversationMessageReceipt(chatServiceSid, conversationSid, messageSid, pageSize, page, pageToken, requestOptions) {
        const req = this.createRequest('GET');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            chatServiceSid: [chatServiceSid, string()],
            conversationSid: [conversationSid, string()],
            messageSid: [messageSid, string()],
            pageSize: [pageSize, optional(bigint())],
            page: [page, optional(number())],
            pageToken: [pageToken, optional(string())],
        });
        req.query('PageSize', mapped.pageSize);
        req.query('Page', mapped.page);
        req.query('PageToken', mapped.pageToken);
        req.appendTemplatePath `/v1/Services/${mapped.chatServiceSid}/Conversations/${mapped.conversationSid}/Messages/${mapped.messageSid}/Receipts`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(listServiceConversationMessageReceiptResponseSchema, requestOptions);
    }
}
//# sourceMappingURL=conversationsV1DeliveryReceiptApi.js.map