/**
 * Twilio APIsLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */
import { listServiceUserConversationResponseSchema, } from '../models/listServiceUserConversationResponse.js';
import { listUserConversationResponseSchema, } from '../models/listUserConversationResponse.js';
import { serviceUserConversationSchema, } from '../models/serviceUserConversation.js';
import { serviceUserConversationNotificationLevelSchema, } from '../models/serviceUserConversationNotificationLevel.js';
import { userConversationSchema, } from '../models/userConversation.js';
import { userConversationNotificationLevelSchema, } from '../models/userConversationNotificationLevel.js';
import { bigint, nullable, number, optional, string } from '../schema.js';
import { BaseApi } from './baseApi.js';
export class ConversationsV1UserConversationApi extends BaseApi {
    /**
     * Update a specific User Conversation.
     *
     * @param chatServiceSid       The SID of the [Conversation
     *                                                                         Service](https://www.twilio.
     *                                                                         com/docs/conversations/api/service-
     *                                                                         resource) the Conversation resource is
     *                                                                         associated with.
     * @param userSid              The unique SID identifier of the [User
     *                                                                         resource](https://www.twilio.
     *                                                                         com/docs/conversations/api/user-resource).
     *                                                                         This value can be either the `sid` or the
     *                                                                         `identity` of the User resource.
     * @param conversationSid      The unique SID identifier of the
     *                                                                         Conversation. This value can be either the
     *                                                                         `sid` or the `unique_name` of the
     *                                                                         [Conversation resource](https://www.twilio.
     *                                                                         com/docs/conversations/api/conversation-
     *                                                                         resource).
     * @param notificationLevel    The Notification Level of this User
     *                                                                         Conversation. One of `default` or `muted`.
     * @param lastReadTimestamp    The date of the last message read in
     *                                                                         conversation by the user, given in ISO
     *                                                                         8601 format.
     * @param lastReadMessageIndex The index of the last Message in the
     *                                                                         Conversation that the Participant has read.
     * @return Response from the API call
     */
    async updateServiceUserConversation(chatServiceSid, userSid, conversationSid, notificationLevel, lastReadTimestamp, lastReadMessageIndex, requestOptions) {
        const req = this.createRequest('POST');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            chatServiceSid: [chatServiceSid, string()],
            userSid: [userSid, string()],
            conversationSid: [conversationSid, string()],
            notificationLevel: [
                notificationLevel,
                optional(serviceUserConversationNotificationLevelSchema),
            ],
            lastReadTimestamp: [lastReadTimestamp, optional(string())],
            lastReadMessageIndex: [
                lastReadMessageIndex,
                optional(nullable(number())),
            ],
        });
        req.header('Content-Type', 'application/x-www-form-urlencoded');
        req.form({
            NotificationLevel: mapped.notificationLevel,
            LastReadTimestamp: mapped.lastReadTimestamp,
            LastReadMessageIndex: mapped.lastReadMessageIndex,
        });
        req.appendTemplatePath `/v1/Services/${mapped.chatServiceSid}/Users/${mapped.userSid}/Conversations/${mapped.conversationSid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(serviceUserConversationSchema, requestOptions);
    }
    /**
     * Delete a specific User Conversation.
     *
     * @param chatServiceSid  The SID of the [Conversation Service](https://www.twilio.
     *                                  com/docs/conversations/api/service-resource) the Conversation resource is
     *                                  associated with.
     * @param userSid         The unique SID identifier of the [User resource](https://www.twilio.
     *                                  com/docs/conversations/api/user-resource). This value can be either the `sid` or
     *                                  the `identity` of the User resource.
     * @param conversationSid The unique SID identifier of the Conversation. This value can be either the
     *                                  `sid` or the `unique_name` of the [Conversation resource](https://www.twilio.
     *                                  com/docs/conversations/api/conversation-resource).
     * @return Response from the API call
     */
    async deleteServiceUserConversation(chatServiceSid, userSid, conversationSid, requestOptions) {
        const req = this.createRequest('DELETE');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            chatServiceSid: [chatServiceSid, string()],
            userSid: [userSid, string()],
            conversationSid: [conversationSid, string()],
        });
        req.appendTemplatePath `/v1/Services/${mapped.chatServiceSid}/Users/${mapped.userSid}/Conversations/${mapped.conversationSid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.call(requestOptions);
    }
    /**
     * Fetch a specific User Conversation.
     *
     * @param chatServiceSid  The SID of the [Conversation Service](https://www.twilio.
     *                                  com/docs/conversations/api/service-resource) the Conversation resource is
     *                                  associated with.
     * @param userSid         The unique SID identifier of the [User resource](https://www.twilio.
     *                                  com/docs/conversations/api/user-resource). This value can be either the `sid` or
     *                                  the `identity` of the User resource.
     * @param conversationSid The unique SID identifier of the Conversation. This value can be either the
     *                                  `sid` or the `unique_name` of the [Conversation resource](https://www.twilio.
     *                                  com/docs/conversations/api/conversation-resource).
     * @return Response from the API call
     */
    async fetchServiceUserConversation(chatServiceSid, userSid, conversationSid, requestOptions) {
        const req = this.createRequest('GET');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            chatServiceSid: [chatServiceSid, string()],
            userSid: [userSid, string()],
            conversationSid: [conversationSid, string()],
        });
        req.appendTemplatePath `/v1/Services/${mapped.chatServiceSid}/Users/${mapped.userSid}/Conversations/${mapped.conversationSid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(serviceUserConversationSchema, requestOptions);
    }
    /**
     * Retrieve a list of all User Conversations for the User.
     *
     * @param chatServiceSid The SID of the [Conversation Service](https://www.twilio.
     *                                 com/docs/conversations/api/service-resource) the Conversation resource is
     *                                 associated with.
     * @param userSid        The unique SID identifier of the [User resource](https://www.twilio.
     *                                 com/docs/conversations/api/user-resource). This value can be either the `sid` or
     *                                 the `identity` of the User resource.
     * @param pageSize       How many resources to return in each list page. The default is 50, and the
     *                                 maximum is 50.
     * @param page           The page index. This value is simply for client state.
     * @param pageToken      The page token. This is provided by the API.
     * @return Response from the API call
     */
    async listServiceUserConversation(chatServiceSid, userSid, pageSize, page, pageToken, requestOptions) {
        const req = this.createRequest('GET');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            chatServiceSid: [chatServiceSid, string()],
            userSid: [userSid, string()],
            pageSize: [pageSize, optional(bigint())],
            page: [page, optional(number())],
            pageToken: [pageToken, optional(string())],
        });
        req.query('PageSize', mapped.pageSize);
        req.query('Page', mapped.page);
        req.query('PageToken', mapped.pageToken);
        req.appendTemplatePath `/v1/Services/${mapped.chatServiceSid}/Users/${mapped.userSid}/Conversations`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(listServiceUserConversationResponseSchema, requestOptions);
    }
    /**
     * Update a specific User Conversation.
     *
     * @param userSid              The unique SID identifier of the [User
     *                                                                  resource](https://www.twilio.
     *                                                                  com/docs/conversations/api/user-resource). This
     *                                                                  value can be either the `sid` or the `identity`
     *                                                                  of the User resource.
     * @param conversationSid      The unique SID identifier of the Conversation.
     *                                                                  This value can be either the `sid` or the
     *                                                                  `unique_name` of the [Conversation
     *                                                                  resource](https://www.twilio.
     *                                                                  com/docs/conversations/api/conversation-resource).
     * @param notificationLevel    The Notification Level of this User Conversation.
     *                                                                  One of `default` or `muted`.
     * @param lastReadTimestamp    The date of the last message read in
     *                                                                  conversation by the user, given in ISO 8601
     *                                                                  format.
     * @param lastReadMessageIndex The index of the last Message in the
     *                                                                  Conversation that the Participant has read.
     * @return Response from the API call
     */
    async updateUserConversation(userSid, conversationSid, notificationLevel, lastReadTimestamp, lastReadMessageIndex, requestOptions) {
        const req = this.createRequest('POST');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            userSid: [userSid, string()],
            conversationSid: [conversationSid, string()],
            notificationLevel: [
                notificationLevel,
                optional(userConversationNotificationLevelSchema),
            ],
            lastReadTimestamp: [lastReadTimestamp, optional(string())],
            lastReadMessageIndex: [
                lastReadMessageIndex,
                optional(nullable(number())),
            ],
        });
        req.header('Content-Type', 'application/x-www-form-urlencoded');
        req.form({
            NotificationLevel: mapped.notificationLevel,
            LastReadTimestamp: mapped.lastReadTimestamp,
            LastReadMessageIndex: mapped.lastReadMessageIndex,
        });
        req.appendTemplatePath `/v1/Users/${mapped.userSid}/Conversations/${mapped.conversationSid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(userConversationSchema, requestOptions);
    }
    /**
     * Delete a specific User Conversation.
     *
     * @param userSid         The unique SID identifier of the [User resource](https://www.twilio.
     *                                  com/docs/conversations/api/user-resource). This value can be either the `sid` or
     *                                  the `identity` of the User resource.
     * @param conversationSid The unique SID identifier of the Conversation. This value can be either the
     *                                  `sid` or the `unique_name` of the [Conversation resource](https://www.twilio.
     *                                  com/docs/conversations/api/conversation-resource).
     * @return Response from the API call
     */
    async deleteUserConversation(userSid, conversationSid, requestOptions) {
        const req = this.createRequest('DELETE');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            userSid: [userSid, string()],
            conversationSid: [conversationSid, string()],
        });
        req.appendTemplatePath `/v1/Users/${mapped.userSid}/Conversations/${mapped.conversationSid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.call(requestOptions);
    }
    /**
     * Fetch a specific User Conversation.
     *
     * @param userSid         The unique SID identifier of the [User resource](https://www.twilio.
     *                                  com/docs/conversations/api/user-resource). This value can be either the `sid` or
     *                                  the `identity` of the User resource.
     * @param conversationSid The unique SID identifier of the Conversation. This value can be either the
     *                                  `sid` or the `unique_name` of the [Conversation resource](https://www.twilio.
     *                                  com/docs/conversations/api/conversation-resource).
     * @return Response from the API call
     */
    async fetchUserConversation(userSid, conversationSid, requestOptions) {
        const req = this.createRequest('GET');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            userSid: [userSid, string()],
            conversationSid: [conversationSid, string()],
        });
        req.appendTemplatePath `/v1/Users/${mapped.userSid}/Conversations/${mapped.conversationSid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(userConversationSchema, requestOptions);
    }
    /**
     * Retrieve a list of all User Conversations for the User.
     *
     * @param userSid   The unique SID identifier of the [User resource](https://www.twilio.
     *                            com/docs/conversations/api/user-resource). This value can be either the `sid` or the
     *                            `identity` of the User resource.
     * @param pageSize  How many resources to return in each list page. The default is 50, and the maximum is
     *                            50.
     * @param page      The page index. This value is simply for client state.
     * @param pageToken The page token. This is provided by the API.
     * @return Response from the API call
     */
    async listUserConversation(userSid, pageSize, page, pageToken, requestOptions) {
        const req = this.createRequest('GET');
        req.baseUrl('default2');
        const mapped = req.prepareArgs({
            userSid: [userSid, string()],
            pageSize: [pageSize, optional(bigint())],
            page: [page, optional(number())],
            pageToken: [pageToken, optional(string())],
        });
        req.query('PageSize', mapped.pageSize);
        req.query('Page', mapped.page);
        req.query('PageToken', mapped.pageToken);
        req.appendTemplatePath `/v1/Users/${mapped.userSid}/Conversations`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(listUserConversationResponseSchema, requestOptions);
    }
}
//# sourceMappingURL=conversationsV1UserConversationApi.js.map