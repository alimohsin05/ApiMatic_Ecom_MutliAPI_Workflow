/**
 * Twilio APIsLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */
import { listTaskChannelResponseSchema, } from '../models/listTaskChannelResponse.js';
import { taskChannelSchema } from '../models/taskChannel.js';
import { bigint, boolean, number, optional, string } from '../schema.js';
import { BaseApi } from './baseApi.js';
export class TaskrouterV1TaskChannelApi extends BaseApi {
    /**
     * Types of tasks
     *
     * @param workspaceSid The SID of the Workspace with the Task Channel to fetch.
     * @param sid          The SID of the Task Channel resource to fetch.
     * @return Response from the API call
     */
    async fetchTaskChannel(workspaceSid, sid, requestOptions) {
        const req = this.createRequest('GET');
        req.baseUrl('default4');
        const mapped = req.prepareArgs({
            workspaceSid: [workspaceSid, string()],
            sid: [sid, string()],
        });
        req.appendTemplatePath `/v1/Workspaces/${mapped.workspaceSid}/TaskChannels/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(taskChannelSchema, requestOptions);
    }
    /**
     * Types of tasks
     *
     * @param workspaceSid            The SID of the Workspace with the Task Channel to update.
     * @param sid                     The SID of the Task Channel resource to update.
     * @param friendlyName            A descriptive string that you create to describe the Task Channel. It
     *                                           can be up to 64 characters long.
     * @param channelOptimizedRouting Whether the TaskChannel should prioritize Workers that have been idle.
     *                                           If `true`, Workers that have been idle the longest are prioritized.
     * @return Response from the API call
     */
    async updateTaskChannel(workspaceSid, sid, friendlyName, channelOptimizedRouting, requestOptions) {
        const req = this.createRequest('POST');
        req.baseUrl('default4');
        const mapped = req.prepareArgs({
            workspaceSid: [workspaceSid, string()],
            sid: [sid, string()],
            friendlyName: [friendlyName, optional(string())],
            channelOptimizedRouting: [channelOptimizedRouting, optional(boolean())],
        });
        req.header('Content-Type', 'application/x-www-form-urlencoded');
        req.form({
            FriendlyName: mapped.friendlyName,
            ChannelOptimizedRouting: mapped.channelOptimizedRouting,
        });
        req.appendTemplatePath `/v1/Workspaces/${mapped.workspaceSid}/TaskChannels/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(taskChannelSchema, requestOptions);
    }
    /**
     * Types of tasks
     *
     * @param workspaceSid The SID of the Workspace with the Task Channel to delete.
     * @param sid          The SID of the Task Channel resource to delete.
     * @return Response from the API call
     */
    async deleteTaskChannel(workspaceSid, sid, requestOptions) {
        const req = this.createRequest('DELETE');
        req.baseUrl('default4');
        const mapped = req.prepareArgs({
            workspaceSid: [workspaceSid, string()],
            sid: [sid, string()],
        });
        req.appendTemplatePath `/v1/Workspaces/${mapped.workspaceSid}/TaskChannels/${mapped.sid}`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.call(requestOptions);
    }
    /**
     * Types of tasks
     *
     * @param workspaceSid The SID of the Workspace with the Task Channel to read.
     * @param pageSize     How many resources to return in each list page. The default is 50, and the maximum
     *                               is 1000.
     * @param page         The page index. This value is simply for client state.
     * @param pageToken    The page token. This is provided by the API.
     * @return Response from the API call
     */
    async listTaskChannel(workspaceSid, pageSize, page, pageToken, requestOptions) {
        const req = this.createRequest('GET');
        req.baseUrl('default4');
        const mapped = req.prepareArgs({
            workspaceSid: [workspaceSid, string()],
            pageSize: [pageSize, optional(bigint())],
            page: [page, optional(number())],
            pageToken: [pageToken, optional(string())],
        });
        req.query('PageSize', mapped.pageSize);
        req.query('Page', mapped.page);
        req.query('PageToken', mapped.pageToken);
        req.appendTemplatePath `/v1/Workspaces/${mapped.workspaceSid}/TaskChannels`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(listTaskChannelResponseSchema, requestOptions);
    }
    /**
     * Types of tasks
     *
     * @param workspaceSid            The SID of the Workspace that the new Task Channel belongs to.
     * @param friendlyName            A descriptive string that you create to describe the Task Channel. It
     *                                           can be up to 64 characters long.
     * @param uniqueName              An application-defined string that uniquely identifies the Task Channel,
     *                                           such as `voice` or `sms`.
     * @param channelOptimizedRouting Whether the Task Channel should prioritize Workers that have been idle.
     *                                           If `true`, Workers that have been idle the longest are prioritized.
     * @return Response from the API call
     */
    async createTaskChannel(workspaceSid, friendlyName, uniqueName, channelOptimizedRouting, requestOptions) {
        const req = this.createRequest('POST');
        req.baseUrl('default4');
        const mapped = req.prepareArgs({
            workspaceSid: [workspaceSid, string()],
            friendlyName: [friendlyName, string()],
            uniqueName: [uniqueName, string()],
            channelOptimizedRouting: [channelOptimizedRouting, optional(boolean())],
        });
        req.header('Content-Type', 'application/x-www-form-urlencoded');
        req.form({
            FriendlyName: mapped.friendlyName,
            UniqueName: mapped.uniqueName,
            ChannelOptimizedRouting: mapped.channelOptimizedRouting,
        });
        req.appendTemplatePath `/v1/Workspaces/${mapped.workspaceSid}/TaskChannels`;
        req.authenticate([{ accountSidAuthToken: true }]);
        return req.callAsJson(taskChannelSchema, requestOptions);
    }
}
//# sourceMappingURL=taskrouterV1TaskChannelApi.js.map