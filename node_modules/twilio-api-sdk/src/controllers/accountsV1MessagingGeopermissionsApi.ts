/**
 * Twilio APIsLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ApiResponse, RequestOptions } from '../core.js';
import {
  MsgGeopermissions,
  msgGeopermissionsSchema,
} from '../models/msgGeopermissions.js';
import { array, optional, string, unknown } from '../schema.js';
import { BaseApi } from './baseApi.js';

export class AccountsV1MessagingGeopermissionsApi extends BaseApi {
  /**
   * Manage Geo Permissions for each country.
   *
   * @param permissions  A list of objects where each object represents the Geo Permission to be updated.
   *                                  Each object contains the following fields: `country_code`, unique code for each
   *                                  country of Geo Permission; `type`, permission type of the Geo Permission i.e.
   *                                  country; `enabled`, configure true for enabling the Geo Permission, false for
   *                                  disabling the Geo Permission.
   * @return Response from the API call
   */
  async updateMessagingGeopermissions(
    permissions: unknown[],
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<MsgGeopermissions>> {
    const req = this.createRequest('PATCH', '/v1/Messaging/GeoPermissions');
    const mapped = req.prepareArgs({
      permissions: [permissions, array(unknown())],
    });
    req.header('Content-Type', 'application/x-www-form-urlencoded');
    req.form({ Permissions: mapped.permissions });
    req.authenticate([{ accountSidAuthToken: true }]);
    return req.callAsJson(msgGeopermissionsSchema, requestOptions);
  }

  /**
   * Manage Geo Permissions for each country.
   *
   * @param countryCode The country code to filter the geo permissions. If provided, only the geo permission
   *                              for the specified country will be returned.
   * @return Response from the API call
   */
  async fetchMessagingGeopermissions(
    countryCode?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<MsgGeopermissions>> {
    const req = this.createRequest('GET', '/v1/Messaging/GeoPermissions');
    const mapped = req.prepareArgs({
      countryCode: [countryCode, optional(string())],
    });
    req.query('CountryCode', mapped.countryCode);
    req.authenticate([{ accountSidAuthToken: true }]);
    return req.callAsJson(msgGeopermissionsSchema, requestOptions);
  }
}
