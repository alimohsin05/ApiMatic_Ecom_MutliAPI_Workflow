/**
 * Twilio APIsLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ApiResponse, RequestOptions } from '../core.js';
import {
  ListWorkerChannelResponse,
  listWorkerChannelResponseSchema,
} from '../models/listWorkerChannelResponse.js';
import { WorkerChannel, workerChannelSchema } from '../models/workerChannel.js';
import { bigint, boolean, number, optional, string } from '../schema.js';
import { BaseApi } from './baseApi.js';

export class TaskrouterV1WorkerChannelApi extends BaseApi {
  /**
   * @param workspaceSid The SID of the Workspace with the WorkerChannels to read.
   * @param workerSid    The SID of the Worker with the WorkerChannels to read.
   * @param pageSize     How many resources to return in each list page. The default is 50, and the maximum
   *                               is 1000.
   * @param page         The page index. This value is simply for client state.
   * @param pageToken    The page token. This is provided by the API.
   * @return Response from the API call
   */
  async listWorkerChannel(
    workspaceSid: string,
    workerSid: string,
    pageSize?: bigint,
    page?: number,
    pageToken?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ListWorkerChannelResponse>> {
    const req = this.createRequest('GET');
    req.baseUrl('default4');
    const mapped = req.prepareArgs({
      workspaceSid: [workspaceSid, string()],
      workerSid: [workerSid, string()],
      pageSize: [pageSize, optional(bigint())],
      page: [page, optional(number())],
      pageToken: [pageToken, optional(string())],
    });
    req.query('PageSize', mapped.pageSize);
    req.query('Page', mapped.page);
    req.query('PageToken', mapped.pageToken);
    req.appendTemplatePath`/v1/Workspaces/${mapped.workspaceSid}/Workers/${mapped.workerSid}/Channels`;
    req.authenticate([{ accountSidAuthToken: true }]);
    return req.callAsJson(listWorkerChannelResponseSchema, requestOptions);
  }

  /**
   * @param workspaceSid The SID of the Workspace with the WorkerChannel to fetch.
   * @param workerSid    The SID of the Worker with the WorkerChannel to fetch.
   * @param sid          The SID of the WorkerChannel to fetch.
   * @return Response from the API call
   */
  async fetchWorkerChannel(
    workspaceSid: string,
    workerSid: string,
    sid: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<WorkerChannel>> {
    const req = this.createRequest('GET');
    req.baseUrl('default4');
    const mapped = req.prepareArgs({
      workspaceSid: [workspaceSid, string()],
      workerSid: [workerSid, string()],
      sid: [sid, string()],
    });
    req.appendTemplatePath`/v1/Workspaces/${mapped.workspaceSid}/Workers/${mapped.workerSid}/Channels/${mapped.sid}`;
    req.authenticate([{ accountSidAuthToken: true }]);
    return req.callAsJson(workerChannelSchema, requestOptions);
  }

  /**
   * @param workspaceSid The SID of the Workspace with the WorkerChannel to update.
   * @param workerSid    The SID of the Worker with the WorkerChannel to update.
   * @param sid          The SID of the WorkerChannel to update.
   * @param capacity     The total number of Tasks that the Worker should handle for the TaskChannel type.
   *                                TaskRouter creates reservations for Tasks of this TaskChannel type up to the
   *                                specified capacity. If the capacity is 0, no new reservations will be created.
   * @param available    Whether the WorkerChannel is available. Set to `false` to prevent the Worker from
   *                                receiving any new Tasks of this TaskChannel type.
   * @return Response from the API call
   */
  async updateWorkerChannel(
    workspaceSid: string,
    workerSid: string,
    sid: string,
    capacity?: number,
    available?: boolean,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<WorkerChannel>> {
    const req = this.createRequest('POST');
    req.baseUrl('default4');
    const mapped = req.prepareArgs({
      workspaceSid: [workspaceSid, string()],
      workerSid: [workerSid, string()],
      sid: [sid, string()],
      capacity: [capacity, optional(number())],
      available: [available, optional(boolean())],
    });
    req.header('Content-Type', 'application/x-www-form-urlencoded');
    req.form({ Capacity: mapped.capacity, Available: mapped.available });
    req.appendTemplatePath`/v1/Workspaces/${mapped.workspaceSid}/Workers/${mapped.workerSid}/Channels/${mapped.sid}`;
    req.authenticate([{ accountSidAuthToken: true }]);
    return req.callAsJson(workerChannelSchema, requestOptions);
  }
}
