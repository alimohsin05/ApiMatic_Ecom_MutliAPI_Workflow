/**
 * Twilio APIsLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ApiResponse, RequestOptions } from '../core.js';
import {
  MConfiguration,
  mConfigurationSchema,
} from '../models/mConfiguration.js';
import {
  ServiceConfiguration,
  serviceConfigurationSchema,
} from '../models/serviceConfiguration.js';
import { boolean, optional, string } from '../schema.js';
import { BaseApi } from './baseApi.js';

export class ConversationsV1ConfigurationApi extends BaseApi {
  /**
   * Fetch the global configuration of conversations on your account
   *
   * @return Response from the API call
   */
  async fetchConfiguration(
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<MConfiguration>> {
    const req = this.createRequest('GET', '/v1/Configuration');
    req.baseUrl('default2');
    req.authenticate([{ accountSidAuthToken: true }]);
    return req.callAsJson(mConfigurationSchema, requestOptions);
  }

  /**
   * Update the global configuration of conversations on your account
   *
   * @param defaultChatServiceSid      The SID of the default [Conversation Service](https://www.twilio.
   *                                             com/docs/conversations/api/service-resource) to use when creating a
   *                                             conversation.
   * @param defaultMessagingServiceSid The SID of the default [Messaging Service](https://www.twilio.
   *                                             com/docs/messaging/api/service-resource) to use when creating a
   *                                             conversation.
   * @param defaultInactiveTimer       Default ISO8601 duration when conversation will be switched to
   *                                             `inactive` state. Minimum value for this timer is 1 minute.
   * @param defaultClosedTimer         Default ISO8601 duration when conversation will be switched to
   *                                             `closed` state. Minimum value for this timer is 10 minutes.
   * @return Response from the API call
   */
  async updateConfiguration(
    defaultChatServiceSid?: string,
    defaultMessagingServiceSid?: string,
    defaultInactiveTimer?: string,
    defaultClosedTimer?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<MConfiguration>> {
    const req = this.createRequest('POST', '/v1/Configuration');
    req.baseUrl('default2');
    const mapped = req.prepareArgs({
      defaultChatServiceSid: [defaultChatServiceSid, optional(string())],
      defaultMessagingServiceSid: [
        defaultMessagingServiceSid,
        optional(string()),
      ],
      defaultInactiveTimer: [defaultInactiveTimer, optional(string())],
      defaultClosedTimer: [defaultClosedTimer, optional(string())],
    });
    req.header('Content-Type', 'application/x-www-form-urlencoded');
    req.form({
      DefaultChatServiceSid: mapped.defaultChatServiceSid,
      DefaultMessagingServiceSid: mapped.defaultMessagingServiceSid,
      DefaultInactiveTimer: mapped.defaultInactiveTimer,
      DefaultClosedTimer: mapped.defaultClosedTimer,
    });
    req.authenticate([{ accountSidAuthToken: true }]);
    return req.callAsJson(mConfigurationSchema, requestOptions);
  }

  /**
   * Fetch the configuration of a conversation service
   *
   * @param chatServiceSid The SID of the Service configuration resource to fetch.
   * @return Response from the API call
   */
  async fetchServiceConfiguration(
    chatServiceSid: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ServiceConfiguration>> {
    const req = this.createRequest('GET');
    req.baseUrl('default2');
    const mapped = req.prepareArgs({
      chatServiceSid: [chatServiceSid, string()],
    });
    req.appendTemplatePath`/v1/Services/${mapped.chatServiceSid}/Configuration`;
    req.authenticate([{ accountSidAuthToken: true }]);
    return req.callAsJson(serviceConfigurationSchema, requestOptions);
  }

  /**
   * Update configuration settings of a conversation service
   *
   * @param chatServiceSid                    The SID of the Service configuration resource to update.
   * @param defaultConversationCreatorRoleSid The conversation-level role assigned to a conversation
   *                                                     creator when they join a new conversation. See [Conversation
   *                                                     Role](https://www.twilio.com/docs/conversations/api/role-
   *                                                     resource) for more info about roles.
   * @param defaultConversationRoleSid        The conversation-level role assigned to users when they are
   *                                                     added to a conversation. See [Conversation Role](https://www.
   *                                                     twilio.com/docs/conversations/api/role-resource) for more info
   *                                                     about roles.
   * @param defaultChatServiceRoleSid         The service-level role assigned to users when they are added
   *                                                     to the service. See [Conversation Role](https://www.twilio.
   *                                                     com/docs/conversations/api/role-resource) for more info about
   *                                                     roles.
   * @param reachabilityEnabled               Whether the [Reachability Indicator](https://www.twilio.
   *                                                     com/docs/conversations/reachability) is enabled for this
   *                                                     Conversations Service. The default is `false`.
   * @return Response from the API call
   */
  async updateServiceConfiguration(
    chatServiceSid: string,
    defaultConversationCreatorRoleSid?: string,
    defaultConversationRoleSid?: string,
    defaultChatServiceRoleSid?: string,
    reachabilityEnabled?: boolean,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ServiceConfiguration>> {
    const req = this.createRequest('POST');
    req.baseUrl('default2');
    const mapped = req.prepareArgs({
      chatServiceSid: [chatServiceSid, string()],
      defaultConversationCreatorRoleSid: [
        defaultConversationCreatorRoleSid,
        optional(string()),
      ],
      defaultConversationRoleSid: [
        defaultConversationRoleSid,
        optional(string()),
      ],
      defaultChatServiceRoleSid: [
        defaultChatServiceRoleSid,
        optional(string()),
      ],
      reachabilityEnabled: [reachabilityEnabled, optional(boolean())],
    });
    req.header('Content-Type', 'application/x-www-form-urlencoded');
    req.form({
      DefaultConversationCreatorRoleSid: mapped.defaultConversationCreatorRoleSid,
      DefaultConversationRoleSid: mapped.defaultConversationRoleSid,
      DefaultChatServiceRoleSid: mapped.defaultChatServiceRoleSid,
      ReachabilityEnabled: mapped.reachabilityEnabled,
    });
    req.appendTemplatePath`/v1/Services/${mapped.chatServiceSid}/Configuration`;
    req.authenticate([{ accountSidAuthToken: true }]);
    return req.callAsJson(serviceConfigurationSchema, requestOptions);
  }
}
