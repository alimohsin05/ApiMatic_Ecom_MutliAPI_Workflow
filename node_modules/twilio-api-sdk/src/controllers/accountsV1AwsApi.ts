/**
 * Twilio APIsLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ApiResponse, RequestOptions } from '../core.js';
import { CredentialAws, credentialAwsSchema } from '../models/credentialAws.js';
import {
  ListCredentialAwsResponse,
  listCredentialAwsResponseSchema,
} from '../models/listCredentialAwsResponse.js';
import { bigint, number, optional, string } from '../schema.js';
import { BaseApi } from './baseApi.js';

export class AccountsV1AwsApi extends BaseApi {
  /**
   * Retrieves a collection of AWS Credentials belonging to the account used to make the request
   *
   * @param pageSize  How many resources to return in each list page. The default is 50, and the maximum is
   *                            1000.
   * @param page      The page index. This value is simply for client state.
   * @param pageToken The page token. This is provided by the API.
   * @return Response from the API call
   */
  async listCredentialAws(
    pageSize?: bigint,
    page?: number,
    pageToken?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ListCredentialAwsResponse>> {
    const req = this.createRequest('GET', '/v1/Credentials/AWS');
    const mapped = req.prepareArgs({
      pageSize: [pageSize, optional(bigint())],
      page: [page, optional(number())],
      pageToken: [pageToken, optional(string())],
    });
    req.query('PageSize', mapped.pageSize);
    req.query('Page', mapped.page);
    req.query('PageToken', mapped.pageToken);
    req.authenticate([{ accountSidAuthToken: true }]);
    return req.callAsJson(listCredentialAwsResponseSchema, requestOptions);
  }

  /**
   * Create a new AWS Credential
   *
   * @param credentials  A string that contains the AWS access credentials in the format
   *                               `<AWS_ACCESS_KEY_ID>:<AWS_SECRET_ACCESS_KEY>`. For example, `AKIAIOSFODNN7EXAMPLE:
   *                               wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY`
   * @param friendlyName A descriptive string that you create to describe the resource. It can be up to 64
   *                               characters long.
   * @param accountSid   The SID of the Subaccount that this Credential should be associated with. Must be a
   *                               valid Subaccount of the account issuing the request.
   * @return Response from the API call
   */
  async createCredentialAws(
    credentials: string,
    friendlyName?: string,
    accountSid?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<CredentialAws>> {
    const req = this.createRequest('POST', '/v1/Credentials/AWS');
    const mapped = req.prepareArgs({
      credentials: [credentials, string()],
      friendlyName: [friendlyName, optional(string())],
      accountSid: [accountSid, optional(string())],
    });
    req.header('Content-Type', 'application/x-www-form-urlencoded');
    req.form({
      Credentials: mapped.credentials,
      FriendlyName: mapped.friendlyName,
      AccountSid: mapped.accountSid,
    });
    req.authenticate([{ accountSidAuthToken: true }]);
    return req.callAsJson(credentialAwsSchema, requestOptions);
  }

  /**
   * Fetch the AWS credentials specified by the provided Credential Sid
   *
   * @param sid The Twilio-provided string that uniquely identifies the AWS resource to fetch.
   * @return Response from the API call
   */
  async fetchCredentialAws(
    sid: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<CredentialAws>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({ sid: [sid, string()] });
    req.appendTemplatePath`/v1/Credentials/AWS/${mapped.sid}`;
    req.authenticate([{ accountSidAuthToken: true }]);
    return req.callAsJson(credentialAwsSchema, requestOptions);
  }

  /**
   * Modify the properties of a given Account
   *
   * @param sid          The Twilio-provided string that uniquely identifies the AWS resource to update.
   * @param friendlyName A descriptive string that you create to describe the resource. It can be up to 64
   *                               characters long.
   * @return Response from the API call
   */
  async updateCredentialAws(
    sid: string,
    friendlyName?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<CredentialAws>> {
    const req = this.createRequest('POST');
    const mapped = req.prepareArgs({
      sid: [sid, string()],
      friendlyName: [friendlyName, optional(string())],
    });
    req.header('Content-Type', 'application/x-www-form-urlencoded');
    req.form({ FriendlyName: mapped.friendlyName });
    req.appendTemplatePath`/v1/Credentials/AWS/${mapped.sid}`;
    req.authenticate([{ accountSidAuthToken: true }]);
    return req.callAsJson(credentialAwsSchema, requestOptions);
  }

  /**
   * Delete a Credential from your account
   *
   * @param sid The Twilio-provided string that uniquely identifies the AWS resource to delete.
   * @return Response from the API call
   */
  async deleteCredentialAws(
    sid: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<void>> {
    const req = this.createRequest('DELETE');
    const mapped = req.prepareArgs({ sid: [sid, string()] });
    req.appendTemplatePath`/v1/Credentials/AWS/${mapped.sid}`;
    req.authenticate([{ accountSidAuthToken: true }]);
    return req.call(requestOptions);
  }
}
