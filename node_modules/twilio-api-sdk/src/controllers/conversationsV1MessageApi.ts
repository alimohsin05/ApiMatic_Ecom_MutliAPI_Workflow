/**
 * Twilio APIsLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ApiResponse, RequestOptions } from '../core.js';
import {
  ChannelWebhookEnabledType,
  channelWebhookEnabledTypeSchema,
} from '../models/channelWebhookEnabledType.js';
import {
  ConversationMessage,
  conversationMessageSchema,
} from '../models/conversationMessage.js';
import {
  ConversationMessageOrderType,
  conversationMessageOrderTypeSchema,
} from '../models/conversationMessageOrderType.js';
import {
  ListConversationMessageResponse,
  listConversationMessageResponseSchema,
} from '../models/listConversationMessageResponse.js';
import {
  ListServiceConversationMessageResponse,
  listServiceConversationMessageResponseSchema,
} from '../models/listServiceConversationMessageResponse.js';
import {
  ServiceConversationMessage,
  serviceConversationMessageSchema,
} from '../models/serviceConversationMessage.js';
import {
  ServiceConversationMessageOrderType,
  serviceConversationMessageOrderTypeSchema,
} from '../models/serviceConversationMessageOrderType.js';
import { bigint, number, optional, string } from '../schema.js';
import { BaseApi } from './baseApi.js';

export class ConversationsV1MessageApi extends BaseApi {
  /**
   * Add a new message to the conversation
   *
   * @param conversationSid          The unique ID of the [Conversation](https://www.
   *                                                              twilio.com/docs/conversations/api/conversation-
   *                                                              resource) for this message.
   * @param xTwilioWebhookEnabled    The X-Twilio-Webhook-Enabled HTTP request header
   * @param author                   The channel specific identifier of the message's
   *                                                              author. Defaults to `system`.
   * @param body                     The content of the message, can be up to 1,600
   *                                                              characters long.
   * @param dateCreated              The date that this resource was created.
   * @param dateUpdated              The date that this resource was last updated. `null`
   *                                                              if the message has not been edited.
   * @param attributes               A string metadata field you can use to store any
   *                                                              data you wish. The string value must contain
   *                                                              structurally valid JSON if specified.  **Note** that
   *                                                              if the attributes are not set "{}" will be returned.
   * @param mediaSid                 The Media SID to be attached to the new Message.
   * @param contentSid               The unique ID of the multi-channel [Rich
   *                                                              Content](https://www.twilio.com/docs/content)
   *                                                              template, required for template-generated messages.
   *                                                              **Note** that if this field is set, `Body` and
   *                                                              `MediaSid` parameters are ignored.
   * @param contentVariables         A structurally valid JSON string that contains
   *                                                              values to resolve Rich Content template variables.
   * @param subject                  The subject of the message, can be up to 256
   *                                                              characters long.
   * @return Response from the API call
   */
  async createConversationMessage(
    conversationSid: string,
    xTwilioWebhookEnabled?: ChannelWebhookEnabledType,
    author?: string,
    body?: string,
    dateCreated?: string,
    dateUpdated?: string,
    attributes?: string,
    mediaSid?: string,
    contentSid?: string,
    contentVariables?: string,
    subject?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ConversationMessage>> {
    const req = this.createRequest('POST');
    req.baseUrl('default2');
    const mapped = req.prepareArgs({
      conversationSid: [conversationSid, string()],
      xTwilioWebhookEnabled: [
        xTwilioWebhookEnabled,
        optional(channelWebhookEnabledTypeSchema),
      ],
      author: [author, optional(string())],
      body: [body, optional(string())],
      dateCreated: [dateCreated, optional(string())],
      dateUpdated: [dateUpdated, optional(string())],
      attributes: [attributes, optional(string())],
      mediaSid: [mediaSid, optional(string())],
      contentSid: [contentSid, optional(string())],
      contentVariables: [contentVariables, optional(string())],
      subject: [subject, optional(string())],
    });
    req.header('Content-Type', 'application/x-www-form-urlencoded');
    req.header('X-Twilio-Webhook-Enabled', mapped.xTwilioWebhookEnabled);
    req.form({
      Author: mapped.author,
      Body: mapped.body,
      DateCreated: mapped.dateCreated,
      DateUpdated: mapped.dateUpdated,
      Attributes: mapped.attributes,
      MediaSid: mapped.mediaSid,
      ContentSid: mapped.contentSid,
      ContentVariables: mapped.contentVariables,
      Subject: mapped.subject,
    });
    req.appendTemplatePath`/v1/Conversations/${mapped.conversationSid}/Messages`;
    req.authenticate([{ accountSidAuthToken: true }]);
    return req.callAsJson(conversationMessageSchema, requestOptions);
  }

  /**
   * Retrieve a list of all messages in the conversation
   *
   * @param conversationSid The unique ID of the [Conversation](https://www.twilio.
   *                                                        com/docs/conversations/api/conversation-resource) for
   *                                                        messages.
   * @param order           The sort order of the returned messages. Can be: `asc`
   *                                                        (ascending) or `desc` (descending), with `asc` as the
   *                                                        default.
   * @param pageSize        How many resources to return in each list page. The
   *                                                        default is 50, and the maximum is 100.
   * @param page            The page index. This value is simply for client state.
   * @param pageToken       The page token. This is provided by the API.
   * @return Response from the API call
   */
  async listConversationMessage(
    conversationSid: string,
    order?: ConversationMessageOrderType,
    pageSize?: bigint,
    page?: number,
    pageToken?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ListConversationMessageResponse>> {
    const req = this.createRequest('GET');
    req.baseUrl('default2');
    const mapped = req.prepareArgs({
      conversationSid: [conversationSid, string()],
      order: [order, optional(conversationMessageOrderTypeSchema)],
      pageSize: [pageSize, optional(bigint())],
      page: [page, optional(number())],
      pageToken: [pageToken, optional(string())],
    });
    req.query('Order', mapped.order);
    req.query('PageSize', mapped.pageSize);
    req.query('Page', mapped.page);
    req.query('PageToken', mapped.pageToken);
    req.appendTemplatePath`/v1/Conversations/${mapped.conversationSid}/Messages`;
    req.authenticate([{ accountSidAuthToken: true }]);
    return req.callAsJson(
      listConversationMessageResponseSchema,
      requestOptions
    );
  }

  /**
   * Update an existing message in the conversation
   *
   * @param conversationSid          The unique ID of the [Conversation](https://www.
   *                                                              twilio.com/docs/conversations/api/conversation-
   *                                                              resource) for this message.
   * @param sid                      A 34 character string that uniquely identifies this
   *                                                              resource.
   * @param xTwilioWebhookEnabled    The X-Twilio-Webhook-Enabled HTTP request header
   * @param author                   The channel specific identifier of the message's
   *                                                              author. Defaults to `system`.
   * @param body                     The content of the message, can be up to 1,600
   *                                                              characters long.
   * @param dateCreated              The date that this resource was created.
   * @param dateUpdated              The date that this resource was last updated. `null`
   *                                                              if the message has not been edited.
   * @param attributes               A string metadata field you can use to store any
   *                                                              data you wish. The string value must contain
   *                                                              structurally valid JSON if specified.  **Note** that
   *                                                              if the attributes are not set "{}" will be returned.
   * @param subject                  The subject of the message, can be up to 256
   *                                                              characters long.
   * @return Response from the API call
   */
  async updateConversationMessage(
    conversationSid: string,
    sid: string,
    xTwilioWebhookEnabled?: ChannelWebhookEnabledType,
    author?: string,
    body?: string,
    dateCreated?: string,
    dateUpdated?: string,
    attributes?: string,
    subject?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ConversationMessage>> {
    const req = this.createRequest('POST');
    req.baseUrl('default2');
    const mapped = req.prepareArgs({
      conversationSid: [conversationSid, string()],
      sid: [sid, string()],
      xTwilioWebhookEnabled: [
        xTwilioWebhookEnabled,
        optional(channelWebhookEnabledTypeSchema),
      ],
      author: [author, optional(string())],
      body: [body, optional(string())],
      dateCreated: [dateCreated, optional(string())],
      dateUpdated: [dateUpdated, optional(string())],
      attributes: [attributes, optional(string())],
      subject: [subject, optional(string())],
    });
    req.header('Content-Type', 'application/x-www-form-urlencoded');
    req.header('X-Twilio-Webhook-Enabled', mapped.xTwilioWebhookEnabled);
    req.form({
      Author: mapped.author,
      Body: mapped.body,
      DateCreated: mapped.dateCreated,
      DateUpdated: mapped.dateUpdated,
      Attributes: mapped.attributes,
      Subject: mapped.subject,
    });
    req.appendTemplatePath`/v1/Conversations/${mapped.conversationSid}/Messages/${mapped.sid}`;
    req.authenticate([{ accountSidAuthToken: true }]);
    return req.callAsJson(conversationMessageSchema, requestOptions);
  }

  /**
   * Remove a message from the conversation
   *
   * @param conversationSid          The unique ID of the [Conversation](https://www.
   *                                                              twilio.com/docs/conversations/api/conversation-
   *                                                              resource) for this message.
   * @param sid                      A 34 character string that uniquely identifies this
   *                                                              resource.
   * @param xTwilioWebhookEnabled    The X-Twilio-Webhook-Enabled HTTP request header
   * @return Response from the API call
   */
  async deleteConversationMessage(
    conversationSid: string,
    sid: string,
    xTwilioWebhookEnabled?: ChannelWebhookEnabledType,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<void>> {
    const req = this.createRequest('DELETE');
    req.baseUrl('default2');
    const mapped = req.prepareArgs({
      conversationSid: [conversationSid, string()],
      sid: [sid, string()],
      xTwilioWebhookEnabled: [
        xTwilioWebhookEnabled,
        optional(channelWebhookEnabledTypeSchema),
      ],
    });
    req.header('X-Twilio-Webhook-Enabled', mapped.xTwilioWebhookEnabled);
    req.appendTemplatePath`/v1/Conversations/${mapped.conversationSid}/Messages/${mapped.sid}`;
    req.authenticate([{ accountSidAuthToken: true }]);
    return req.call(requestOptions);
  }

  /**
   * Fetch a message from the conversation
   *
   * @param conversationSid The unique ID of the [Conversation](https://www.twilio.
   *                                  com/docs/conversations/api/conversation-resource) for this message.
   * @param sid             A 34 character string that uniquely identifies this resource.
   * @return Response from the API call
   */
  async fetchConversationMessage(
    conversationSid: string,
    sid: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ConversationMessage>> {
    const req = this.createRequest('GET');
    req.baseUrl('default2');
    const mapped = req.prepareArgs({
      conversationSid: [conversationSid, string()],
      sid: [sid, string()],
    });
    req.appendTemplatePath`/v1/Conversations/${mapped.conversationSid}/Messages/${mapped.sid}`;
    req.authenticate([{ accountSidAuthToken: true }]);
    return req.callAsJson(conversationMessageSchema, requestOptions);
  }

  /**
   * Add a new message to the conversation in a specific service
   *
   * @param chatServiceSid           The SID of the [Conversation Service](https://www.
   *                                                              twilio.com/docs/conversations/api/service-resource)
   *                                                              the Participant resource is associated with.
   * @param conversationSid          The unique ID of the [Conversation](https://www.
   *                                                              twilio.com/docs/conversations/api/conversation-
   *                                                              resource) for this message.
   * @param xTwilioWebhookEnabled    The X-Twilio-Webhook-Enabled HTTP request header
   * @param author                   The channel specific identifier of the message's
   *                                                              author. Defaults to `system`.
   * @param body                     The content of the message, can be up to 1,600
   *                                                              characters long.
   * @param dateCreated              The date that this resource was created.
   * @param dateUpdated              The date that this resource was last updated. `null`
   *                                                              if the message has not been edited.
   * @param attributes               A string metadata field you can use to store any
   *                                                              data you wish. The string value must contain
   *                                                              structurally valid JSON if specified.  **Note** that
   *                                                              if the attributes are not set "{}" will be returned.
   * @param mediaSid                 The Media SID to be attached to the new Message.
   * @param contentSid               The unique ID of the multi-channel [Rich
   *                                                              Content](https://www.twilio.com/docs/content)
   *                                                              template, required for template-generated messages.
   *                                                              **Note** that if this field is set, `Body` and
   *                                                              `MediaSid` parameters are ignored.
   * @param contentVariables         A structurally valid JSON string that contains
   *                                                              values to resolve Rich Content template variables.
   * @param subject                  The subject of the message, can be up to 256
   *                                                              characters long.
   * @return Response from the API call
   */
  async createServiceConversationMessage(
    chatServiceSid: string,
    conversationSid: string,
    xTwilioWebhookEnabled?: ChannelWebhookEnabledType,
    author?: string,
    body?: string,
    dateCreated?: string,
    dateUpdated?: string,
    attributes?: string,
    mediaSid?: string,
    contentSid?: string,
    contentVariables?: string,
    subject?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ServiceConversationMessage>> {
    const req = this.createRequest('POST');
    req.baseUrl('default2');
    const mapped = req.prepareArgs({
      chatServiceSid: [chatServiceSid, string()],
      conversationSid: [conversationSid, string()],
      xTwilioWebhookEnabled: [
        xTwilioWebhookEnabled,
        optional(channelWebhookEnabledTypeSchema),
      ],
      author: [author, optional(string())],
      body: [body, optional(string())],
      dateCreated: [dateCreated, optional(string())],
      dateUpdated: [dateUpdated, optional(string())],
      attributes: [attributes, optional(string())],
      mediaSid: [mediaSid, optional(string())],
      contentSid: [contentSid, optional(string())],
      contentVariables: [contentVariables, optional(string())],
      subject: [subject, optional(string())],
    });
    req.header('Content-Type', 'application/x-www-form-urlencoded');
    req.header('X-Twilio-Webhook-Enabled', mapped.xTwilioWebhookEnabled);
    req.form({
      Author: mapped.author,
      Body: mapped.body,
      DateCreated: mapped.dateCreated,
      DateUpdated: mapped.dateUpdated,
      Attributes: mapped.attributes,
      MediaSid: mapped.mediaSid,
      ContentSid: mapped.contentSid,
      ContentVariables: mapped.contentVariables,
      Subject: mapped.subject,
    });
    req.appendTemplatePath`/v1/Services/${mapped.chatServiceSid}/Conversations/${mapped.conversationSid}/Messages`;
    req.authenticate([{ accountSidAuthToken: true }]);
    return req.callAsJson(serviceConversationMessageSchema, requestOptions);
  }

  /**
   * Retrieve a list of all messages in the conversation
   *
   * @param chatServiceSid  The SID of the [Conversation Service](https://www.
   *                                                               twilio.com/docs/conversations/api/service-resource)
   *                                                               the Participant resource is associated with.
   * @param conversationSid The unique ID of the [Conversation](https://www.
   *                                                               twilio.com/docs/conversations/api/conversation-
   *                                                               resource) for messages.
   * @param order           The sort order of the returned messages. Can be:
   *                                                               `asc` (ascending) or `desc` (descending), with `asc`
   *                                                               as the default.
   * @param pageSize        How many resources to return in each list page. The
   *                                                               default is 50, and the maximum is 100.
   * @param page            The page index. This value is simply for client
   *                                                               state.
   * @param pageToken       The page token. This is provided by the API.
   * @return Response from the API call
   */
  async listServiceConversationMessage(
    chatServiceSid: string,
    conversationSid: string,
    order?: ServiceConversationMessageOrderType,
    pageSize?: bigint,
    page?: number,
    pageToken?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ListServiceConversationMessageResponse>> {
    const req = this.createRequest('GET');
    req.baseUrl('default2');
    const mapped = req.prepareArgs({
      chatServiceSid: [chatServiceSid, string()],
      conversationSid: [conversationSid, string()],
      order: [order, optional(serviceConversationMessageOrderTypeSchema)],
      pageSize: [pageSize, optional(bigint())],
      page: [page, optional(number())],
      pageToken: [pageToken, optional(string())],
    });
    req.query('Order', mapped.order);
    req.query('PageSize', mapped.pageSize);
    req.query('Page', mapped.page);
    req.query('PageToken', mapped.pageToken);
    req.appendTemplatePath`/v1/Services/${mapped.chatServiceSid}/Conversations/${mapped.conversationSid}/Messages`;
    req.authenticate([{ accountSidAuthToken: true }]);
    return req.callAsJson(
      listServiceConversationMessageResponseSchema,
      requestOptions
    );
  }

  /**
   * Update an existing message in the conversation
   *
   * @param chatServiceSid           The SID of the [Conversation Service](https://www.
   *                                                              twilio.com/docs/conversations/api/service-resource)
   *                                                              the Participant resource is associated with.
   * @param conversationSid          The unique ID of the [Conversation](https://www.
   *                                                              twilio.com/docs/conversations/api/conversation-
   *                                                              resource) for this message.
   * @param sid                      A 34 character string that uniquely identifies this
   *                                                              resource.
   * @param xTwilioWebhookEnabled    The X-Twilio-Webhook-Enabled HTTP request header
   * @param author                   The channel specific identifier of the message's
   *                                                              author. Defaults to `system`.
   * @param body                     The content of the message, can be up to 1,600
   *                                                              characters long.
   * @param dateCreated              The date that this resource was created.
   * @param dateUpdated              The date that this resource was last updated. `null`
   *                                                              if the message has not been edited.
   * @param attributes               A string metadata field you can use to store any
   *                                                              data you wish. The string value must contain
   *                                                              structurally valid JSON if specified.  **Note** that
   *                                                              if the attributes are not set "{}" will be returned.
   * @param subject                  The subject of the message, can be up to 256
   *                                                              characters long.
   * @return Response from the API call
   */
  async updateServiceConversationMessage(
    chatServiceSid: string,
    conversationSid: string,
    sid: string,
    xTwilioWebhookEnabled?: ChannelWebhookEnabledType,
    author?: string,
    body?: string,
    dateCreated?: string,
    dateUpdated?: string,
    attributes?: string,
    subject?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ServiceConversationMessage>> {
    const req = this.createRequest('POST');
    req.baseUrl('default2');
    const mapped = req.prepareArgs({
      chatServiceSid: [chatServiceSid, string()],
      conversationSid: [conversationSid, string()],
      sid: [sid, string()],
      xTwilioWebhookEnabled: [
        xTwilioWebhookEnabled,
        optional(channelWebhookEnabledTypeSchema),
      ],
      author: [author, optional(string())],
      body: [body, optional(string())],
      dateCreated: [dateCreated, optional(string())],
      dateUpdated: [dateUpdated, optional(string())],
      attributes: [attributes, optional(string())],
      subject: [subject, optional(string())],
    });
    req.header('Content-Type', 'application/x-www-form-urlencoded');
    req.header('X-Twilio-Webhook-Enabled', mapped.xTwilioWebhookEnabled);
    req.form({
      Author: mapped.author,
      Body: mapped.body,
      DateCreated: mapped.dateCreated,
      DateUpdated: mapped.dateUpdated,
      Attributes: mapped.attributes,
      Subject: mapped.subject,
    });
    req.appendTemplatePath`/v1/Services/${mapped.chatServiceSid}/Conversations/${mapped.conversationSid}/Messages/${mapped.sid}`;
    req.authenticate([{ accountSidAuthToken: true }]);
    return req.callAsJson(serviceConversationMessageSchema, requestOptions);
  }

  /**
   * Remove a message from the conversation
   *
   * @param chatServiceSid           The SID of the [Conversation Service](https://www.
   *                                                              twilio.com/docs/conversations/api/service-resource)
   *                                                              the Participant resource is associated with.
   * @param conversationSid          The unique ID of the [Conversation](https://www.
   *                                                              twilio.com/docs/conversations/api/conversation-
   *                                                              resource) for this message.
   * @param sid                      A 34 character string that uniquely identifies this
   *                                                              resource.
   * @param xTwilioWebhookEnabled    The X-Twilio-Webhook-Enabled HTTP request header
   * @return Response from the API call
   */
  async deleteServiceConversationMessage(
    chatServiceSid: string,
    conversationSid: string,
    sid: string,
    xTwilioWebhookEnabled?: ChannelWebhookEnabledType,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<void>> {
    const req = this.createRequest('DELETE');
    req.baseUrl('default2');
    const mapped = req.prepareArgs({
      chatServiceSid: [chatServiceSid, string()],
      conversationSid: [conversationSid, string()],
      sid: [sid, string()],
      xTwilioWebhookEnabled: [
        xTwilioWebhookEnabled,
        optional(channelWebhookEnabledTypeSchema),
      ],
    });
    req.header('X-Twilio-Webhook-Enabled', mapped.xTwilioWebhookEnabled);
    req.appendTemplatePath`/v1/Services/${mapped.chatServiceSid}/Conversations/${mapped.conversationSid}/Messages/${mapped.sid}`;
    req.authenticate([{ accountSidAuthToken: true }]);
    return req.call(requestOptions);
  }

  /**
   * Fetch a message from the conversation
   *
   * @param chatServiceSid  The SID of the [Conversation Service](https://www.twilio.
   *                                  com/docs/conversations/api/service-resource) the Participant resource is
   *                                  associated with.
   * @param conversationSid The unique ID of the [Conversation](https://www.twilio.
   *                                  com/docs/conversations/api/conversation-resource) for this message.
   * @param sid             A 34 character string that uniquely identifies this resource.
   * @return Response from the API call
   */
  async fetchServiceConversationMessage(
    chatServiceSid: string,
    conversationSid: string,
    sid: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ServiceConversationMessage>> {
    const req = this.createRequest('GET');
    req.baseUrl('default2');
    const mapped = req.prepareArgs({
      chatServiceSid: [chatServiceSid, string()],
      conversationSid: [conversationSid, string()],
      sid: [sid, string()],
    });
    req.appendTemplatePath`/v1/Services/${mapped.chatServiceSid}/Conversations/${mapped.conversationSid}/Messages/${mapped.sid}`;
    req.authenticate([{ accountSidAuthToken: true }]);
    return req.callAsJson(serviceConversationMessageSchema, requestOptions);
  }
}
