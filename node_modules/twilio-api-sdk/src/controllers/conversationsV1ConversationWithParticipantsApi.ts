/**
 * Twilio APIsLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ApiResponse, RequestOptions } from '../core.js';
import {
  ChannelWebhookEnabledType,
  channelWebhookEnabledTypeSchema,
} from '../models/channelWebhookEnabledType.js';
import {
  ConversationWithParticipants,
  conversationWithParticipantsSchema,
} from '../models/conversationWithParticipants.js';
import {
  ConversationWithParticipantsState,
  conversationWithParticipantsStateSchema,
} from '../models/conversationWithParticipantsState.js';
import {
  ServiceConversationWithParticipants,
  serviceConversationWithParticipantsSchema,
} from '../models/serviceConversationWithParticipants.js';
import {
  ServiceConversationWithParticipantsState,
  serviceConversationWithParticipantsStateSchema,
} from '../models/serviceConversationWithParticipantsState.js';
import { array, optional, string } from '../schema.js';
import { BaseApi } from './baseApi.js';

export class ConversationsV1ConversationWithParticipantsApi extends BaseApi {
  /**
   * Create a new conversation with the list of participants in your account's default service
   *
   * @param xTwilioWebhookEnabled    The X-Twilio-Webhook-Enabled HTTP request
   *                                                                      header
   * @param friendlyName             The human-readable name of this conversation,
   *                                                                      limited to 256 characters. Optional.
   * @param uniqueName               An application-defined string that uniquely
   *                                                                      identifies the resource. It can be used to
   *                                                                      address the resource in place of the
   *                                                                      resource's `sid` in the URL.
   * @param dateCreated              The date that this resource was created.
   * @param dateUpdated              The date that this resource was last updated.
   * @param messagingServiceSid      The unique ID of the [Messaging
   *                                                                      Service](https://www.twilio.
   *                                                                      com/docs/messaging/api/service-resource) this
   *                                                                      conversation belongs to.
   * @param attributes               An optional string metadata field you can
   *                                                                      use to store any data you wish. The string
   *                                                                      value must contain structurally valid JSON if
   *                                                                      specified.  **Note** that if the attributes
   *                                                                      are not set "{}" will be returned.
   * @param state                    Current state of this conversation. Can be
   *                                                                      either `initializing`, `active`, `inactive`
   *                                                                      or `closed` and defaults to `active`
   * @param timersInactive           ISO8601 duration when conversation will be
   *                                                                      switched to `inactive` state. Minimum value
   *                                                                      for this timer is 1 minute.
   * @param timersClosed             ISO8601 duration when conversation will be
   *                                                                      switched to `closed` state. Minimum value for
   *                                                                      this timer is 10 minutes.
   * @param bindingsEmailAddress     The default email address that will be used
   *                                                                      when sending outbound emails in this
   *                                                                      conversation.
   * @param bindingsEmailName        The default name that will be used when
   *                                                                      sending outbound emails in this conversation.
   * @param participant              The participant to be added to the
   *                                                                      conversation in JSON format. The JSON object
   *                                                                      attributes are as parameters in [Participant
   *                                                                      Resource](https://www.twilio.
   *                                                                      com/docs/conversations/api/conversation-
   *                                                                      participant-resource). The maximum number of
   *                                                                      participants that can be added in a single
   *                                                                      request is 10.
   * @return Response from the API call
   */
  async createConversationWithParticipants(
    xTwilioWebhookEnabled?: ChannelWebhookEnabledType,
    friendlyName?: string,
    uniqueName?: string,
    dateCreated?: string,
    dateUpdated?: string,
    messagingServiceSid?: string,
    attributes?: string,
    state?: ConversationWithParticipantsState,
    timersInactive?: string,
    timersClosed?: string,
    bindingsEmailAddress?: string,
    bindingsEmailName?: string,
    participant?: string[],
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ConversationWithParticipants>> {
    const req = this.createRequest('POST', '/v1/ConversationWithParticipants');
    req.baseUrl('default2');
    const mapped = req.prepareArgs({
      xTwilioWebhookEnabled: [
        xTwilioWebhookEnabled,
        optional(channelWebhookEnabledTypeSchema),
      ],
      friendlyName: [friendlyName, optional(string())],
      uniqueName: [uniqueName, optional(string())],
      dateCreated: [dateCreated, optional(string())],
      dateUpdated: [dateUpdated, optional(string())],
      messagingServiceSid: [messagingServiceSid, optional(string())],
      attributes: [attributes, optional(string())],
      state: [state, optional(conversationWithParticipantsStateSchema)],
      timersInactive: [timersInactive, optional(string())],
      timersClosed: [timersClosed, optional(string())],
      bindingsEmailAddress: [bindingsEmailAddress, optional(string())],
      bindingsEmailName: [bindingsEmailName, optional(string())],
      participant: [participant, optional(array(string()))],
    });
    req.header('Content-Type', 'application/x-www-form-urlencoded');
    req.header('X-Twilio-Webhook-Enabled', mapped.xTwilioWebhookEnabled);
    req.form({
      FriendlyName: mapped.friendlyName,
      UniqueName: mapped.uniqueName,
      DateCreated: mapped.dateCreated,
      DateUpdated: mapped.dateUpdated,
      MessagingServiceSid: mapped.messagingServiceSid,
      Attributes: mapped.attributes,
      State: mapped.state,
      'Timers.Inactive': mapped.timersInactive,
      'Timers.Closed': mapped.timersClosed,
      'Bindings.Email.Address': mapped.bindingsEmailAddress,
      'Bindings.Email.Name': mapped.bindingsEmailName,
      Participant: mapped.participant,
    });
    req.authenticate([{ accountSidAuthToken: true }]);
    return req.callAsJson(conversationWithParticipantsSchema, requestOptions);
  }

  /**
   * Create a new conversation with the list of participants in your account's default service
   *
   * @param chatServiceSid           The SID of the [Conversation
   *                                                                             Service](https://www.twilio.
   *                                                                             com/docs/conversations/api/service-
   *                                                                             resource) the Conversation resource is
   *                                                                             associated with.
   * @param xTwilioWebhookEnabled    The X-Twilio-Webhook-Enabled HTTP
   *                                                                             request header
   * @param friendlyName             The human-readable name of this
   *                                                                             conversation, limited to 256
   *                                                                             characters. Optional.
   * @param uniqueName               An application-defined string that
   *                                                                             uniquely identifies the resource. It
   *                                                                             can be used to address the resource in
   *                                                                             place of the resource's `sid` in the
   *                                                                             URL.
   * @param dateCreated              The date that this resource was
   *                                                                             created.
   * @param dateUpdated              The date that this resource was last
   *                                                                             updated.
   * @param messagingServiceSid      The unique ID of the [Messaging
   *                                                                             Service](https://www.twilio.
   *                                                                             com/docs/messaging/api/service-
   *                                                                             resource) this conversation belongs to.
   * @param attributes               An optional string metadata field you
   *                                                                             can use to store any data you wish.
   *                                                                             The string value must contain
   *                                                                             structurally valid JSON if specified.
   *                                                                             **Note** that if the attributes are
   *                                                                             not set "{}" will be returned.
   * @param state                    Current state of this conversation.
   *                                                                             Can be either `initializing`, `active`,
   *                                                                             `inactive` or `closed` and defaults
   *                                                                             to `active`
   * @param timersInactive           ISO8601 duration when conversation
   *                                                                             will be switched to `inactive` state.
   *                                                                             Minimum value for this timer is 1
   *                                                                             minute.
   * @param timersClosed             ISO8601 duration when conversation
   *                                                                             will be switched to `closed` state.
   *                                                                             Minimum value for this timer is 10
   *                                                                             minutes.
   * @param bindingsEmailAddress     The default email address that will
   *                                                                             be used when sending outbound emails
   *                                                                             in this conversation.
   * @param bindingsEmailName        The default name that will be used
   *                                                                             when sending outbound emails in this
   *                                                                             conversation.
   * @param participant              The participant to be added to the
   *                                                                             conversation in JSON format. The JSON
   *                                                                             object attributes are as parameters in
   *                                                                             [Participant Resource](https://www.
   *                                                                             twilio.
   *                                                                             com/docs/conversations/api/conversatio
   *                                                                             n-participant-resource). The maximum
   *                                                                             number of participants that can be
   *                                                                             added in a single request is 10.
   * @return Response from the API call
   */
  async createServiceConversationWithParticipants(
    chatServiceSid: string,
    xTwilioWebhookEnabled?: ChannelWebhookEnabledType,
    friendlyName?: string,
    uniqueName?: string,
    dateCreated?: string,
    dateUpdated?: string,
    messagingServiceSid?: string,
    attributes?: string,
    state?: ServiceConversationWithParticipantsState,
    timersInactive?: string,
    timersClosed?: string,
    bindingsEmailAddress?: string,
    bindingsEmailName?: string,
    participant?: string[],
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ServiceConversationWithParticipants>> {
    const req = this.createRequest('POST');
    req.baseUrl('default2');
    const mapped = req.prepareArgs({
      chatServiceSid: [chatServiceSid, string()],
      xTwilioWebhookEnabled: [
        xTwilioWebhookEnabled,
        optional(channelWebhookEnabledTypeSchema),
      ],
      friendlyName: [friendlyName, optional(string())],
      uniqueName: [uniqueName, optional(string())],
      dateCreated: [dateCreated, optional(string())],
      dateUpdated: [dateUpdated, optional(string())],
      messagingServiceSid: [messagingServiceSid, optional(string())],
      attributes: [attributes, optional(string())],
      state: [state, optional(serviceConversationWithParticipantsStateSchema)],
      timersInactive: [timersInactive, optional(string())],
      timersClosed: [timersClosed, optional(string())],
      bindingsEmailAddress: [bindingsEmailAddress, optional(string())],
      bindingsEmailName: [bindingsEmailName, optional(string())],
      participant: [participant, optional(array(string()))],
    });
    req.header('Content-Type', 'application/x-www-form-urlencoded');
    req.header('X-Twilio-Webhook-Enabled', mapped.xTwilioWebhookEnabled);
    req.form({
      FriendlyName: mapped.friendlyName,
      UniqueName: mapped.uniqueName,
      DateCreated: mapped.dateCreated,
      DateUpdated: mapped.dateUpdated,
      MessagingServiceSid: mapped.messagingServiceSid,
      Attributes: mapped.attributes,
      State: mapped.state,
      'Timers.Inactive': mapped.timersInactive,
      'Timers.Closed': mapped.timersClosed,
      'Bindings.Email.Address': mapped.bindingsEmailAddress,
      'Bindings.Email.Name': mapped.bindingsEmailName,
      Participant: mapped.participant,
    });
    req.appendTemplatePath`/v1/Services/${mapped.chatServiceSid}/ConversationWithParticipants`;
    req.authenticate([{ accountSidAuthToken: true }]);
    return req.callAsJson(
      serviceConversationWithParticipantsSchema,
      requestOptions
    );
  }
}
